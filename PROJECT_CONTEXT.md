# Project Context — Football Auction Manager

## Project purpose

Build a reliable desktop application for the real Calgary Vipers 2026 football player auction, then polish it into an internship portfolio project. ChatGPT Work maintains requirements, architecture, review, testing guidance, and documentation. Claude Code may continue implementation directly in this repository. The code must remain understandable and should grow milestone by milestone without unnecessary complexity.

## Technology and architecture

- Python desktop application
- CustomTkinter for the later GUI
- Pillow for player images
- JSON persistence during Phase 1
- pytest for automated tests
- Domain models are independent from the UI
- `services/` contains configuration loading, auction-queue randomization, player/team loading + search/filter + setup validation, team-roster resolution/summaries, SOLD/UNSOLD auction transaction logic (with re-auction rounds, the one-GK-per-team rule, and blocked-state detection), the shared in-memory `AuctionSession` (now with MOCK/LIVE mode and autosave), (Milestone 9) `services/persistence_service.py` for save/load/list/delete of JSON session snapshots under `saves/`, (September 2026) `services/auction_history_service.py` for read-only filter/sort/summary of the existing transaction history, (September 2026) `services/report_service.py` for read-only auction/team/price/FPL/OVR/re-auction analytics, (September 2026) `services/preferences_service.py` for organizer app preferences (fullscreen/display, SOLD/UNSOLD confirmation toggles) under `config/` — entirely separate from auction session saves, and (September 2026) `services/live_bid_service.py` (thread-safe live-bid validation, separate from the SOLD/UNSOLD transaction layer) + `services/captain_bidding_server.py` (the local FastAPI/uvicorn phone-bidding server) — see "Captain Phone Bidding — Phase 1 — September 2026" below
- `ui/` has a working CustomTkinter app shell (`main_window.py`, `theme.py`, `widgets.py`), the shared premium card-art system (September 2026) `player_card_art.py` (reused by both the Player Cards gallery and the Live Auction current-player card), and seven real data-driven screens (`ui/screens/live_auction_screen.py`, `ui/screens/player_cards_screen.py`, `ui/screens/players_setup_screen.py`, `ui/screens/teams_screen.py`, `ui/screens/auction_history_screen.py`, `ui/screens/reports_screen.py`, `ui/screens/settings_screen.py`) — every sidebar route now has a real screen; `assets/` holds the official branding logo, all 32 real player photos under `assets/players/`, and (September 2026) the standalone captain-phone web page `assets/captain_bidding/phone.html`
- (September 2026) `fastapi`+`uvicorn` power a lightweight local-network-only web server (`services/captain_bidding_server.py`) so a captain's phone can view live auction state and submit bids over Wi-Fi — see "Captain Phone Bidding — Phase 1 — September 2026" below; this is a controlled, organizer-started local server, not a public web API
- SQLite, React, and analytics dashboards are explicitly deferred

## Official branding

- Overall application/tournament identity: **Calgary Vipers**
- Application name: **Football Auction Manager**
- Intended header title: **Calgary Vipers Auction 2026**
- Official logo: `assets/branding/calgary_vipers_logo.png`
- Current theme key: `vipers_dark`
- Branding configuration lives under `branding` in `data/settings.json`; future UI code must load these values rather than hard-code them
- The official logo should be used for the main app identity, top-left application header, and optional splash/welcome screen
- **Calgary Vipers is not an auction team.** It must never replace or be added to Blackout FC, Darkstar FC, Goli Underdogs, or Showstoppers

## Tournament rules

- Total players: 32
- Teams: 4
- Captains: 4, pre-assigned at zero cost
- Auction-eligible players: 28
- Starting auction budget: 100M per team (400M total)
- Final squad target/maximum: 8 players per team
- Each initial roster contains one captain; each team can buy at most 7 players
- A sale must never make a budget negative or produce a roster larger than 8
- A player cannot be sold twice (SOLD is permanent)
- Captains never receive a base price, sale price, auction sequence, SOLD, or UNSOLD result
- **UNSOLD is never permanent** (Milestone 8.1): a player marked UNSOLD returns in the next re-auction round — a fresh, reshuffled pool of every still-unsold auction-eligible player — and keeps returning in subsequent rounds until sold or until no team can legally take them. The auction is complete only when every auction-eligible player has been SOLD, not merely when one pass through a queue ends.
- **Each team may have at most one goalkeeper** (Milestone 8.1), counting the captain if the captain is a GK. A team that already has a GK cannot buy another. Each final squad is intended to contain exactly one GK; the canonical data (4 auction-eligible GKs, 0 captain GKs, 4 teams) satisfies this exactly.
- **A team at 8/8 players is no longer eligible to buy** (already true from Milestone 7's roster-size check; Milestone 8.1 additionally reflects it proactively in the UI and in re-auction eligibility).
- **A blocked auction state is detected, not looped forever** (Milestone 8.1): if no remaining unsold player can legally go to any team (e.g. every non-full team already has a GK, or every team is full), the auction reports BLOCKED with a readable message instead of endlessly reshuffling.
- **A team must always retain enough budget to fill its remaining roster slots at the minimum legal price** (pre-Milestone-9 budget reserve fix): the minimum legal price for any purchase is 1M; a sale is only legal if the team's budget after paying for it still covers every roster slot still needed afterward, at 1M each. See "PRE-M9 BUDGET RESERVE RULE" below for the formula, worked examples, and enforcement details.

### Teams and captain assignments

| Team | Short name | Captain | Starting budget | Initial squad |
|---|---|---|---:|---:|
| Blackout FC | Samin | Samin Haque | 100M | 1 |
| Darkstar FC | Sabit | Sabit Khan | 100M | 1 |
| Goli Underdogs | Arafat | Arafatul Mamur | 100M | 1 |
| Showstoppers | Riyad | Riyad Zaman | 100M | 1 |

## Current player database

Canonical records live in `data/players.json`. Alternate historical names must not create duplicate records: Rizvi Mahmud = Rizvi Ibrahim; Muhammad Rahmat = Rahmat Ullah; Minhaz Rahman = Minhaz Hamim; Shahriar Arik = Shahriar Anwar Khan.

Refreshed September 2026 (see "Player Data Refresh — September 2026" below) to the organizer-approved final-ish roster: Aldeen, Mirza, and Sarim were removed; Munem was relocated to slot 24 and rerated; Hasnan Siddique Sunve, Farhan Mahin, and Tahsin Islam were added at slots 30–32; Samin Haque moved MID → ATT; all ratings below are now organizer-approved (converted from prior-season performance points, or explicitly organizer-approved where no usable prior-season data existed) rather than editable placeholders. The FPL column (added by "Player Cards + Last-Season FPL — September 2026" below) is the organizer's previous-season FPL point total; `—` means no usable last-season result (stored as `null`/`None`, never `0`).

| # | Player | Short | Pos | OVR | FPL | Captain/team |
|---:|---|---|---|---:|---:|---|
| 1 | Samin Haque | Samin | ATT | 87 | 80 | Blackout FC |
| 2 | Rahmat Ullah | Rahmat | DEF | 88 | 83 | — |
| 3 | Sabit Khan | Sabit | MID | 77 | 47 | Darkstar FC |
| 4 | Shahriar Anwar Khan | Arik | MID | 84 | 70 | — |
| 5 | K M Chisty | Chishty | DEF | 79 | 55 | — |
| 6 | Hussain Yeasin | Yeasin | ATT | 84 | — | — |
| 7 | Ishmam Rahman | Ishmam | ATT | 83 | 65 | — |
| 8 | Md Rafiu Hossain | Rafiu | DEF | 78 | 50 | — |
| 9 | Hasan Mahtab | Mahtab | ATT | 85 | 74 | — |
| 10 | Minhaz Hamim | Minhaz | ATT | 87 | 79 | — |
| 11 | Nabil Shahriar | Nabil | GK | 76 | 42 | — |
| 12 | Adeeb Ahmed | Adeeb | ATT | 84 | 70 | — |
| 13 | Masrur Rahman | Masrur | GK | 88 | — | — |
| 14 | Arafatul Mamur | Arafat | DEF | 76 | 42 | Goli Underdogs |
| 15 | Aafeef Kabir | Afeef | DEF | 79 | 54 | — |
| 16 | Aiman Nawar Chowdhury | Aiman | MID | 80 | 57 | — |
| 17 | Sajid Khalid | Sajid | MID | 75 | — | — |
| 18 | Rayhan | Rayhan | GK | 84 | — | — |
| 19 | Navid Rahman | Navid | ATT | 89 | 88 | — |
| 20 | Rizvi Ibrahim | Rizvi | DEF | 90 | 111 | — |
| 21 | Fairooz Abir | Abir | MID | 86 | 76 | — |
| 22 | Riyad Zaman | Riyad | DEF | 82 | 62 | Showstoppers |
| 23 | Faiad Rehman | Faiad | MID | 85 | 74 | — |
| 24 | Munem | Munem | MID | 89 | — | — |
| 25 | Azmi | Azmi | DEF | 82 | 64 | — |
| 26 | Mubasshir | Mubasshir | DEF | 80 | 57 | — |
| 27 | Farhan Labib | Farhan | DEF | 85 | — | — |
| 28 | Jawad | Jawad | GK | 87 | — | — |
| 29 | Taqi Rahman | Taqi | MID | 75 | 37 | — |
| 30 | Hasnan Siddique Sunve | Sunve | DEF | 82 | — | — |
| 31 | Farhan Mahin | Mahin | ATT | 78 | — | — |
| 32 | Tahsin Islam | Tahsin | ATT | 87 | — | — |

Nabil, Masrur, Rayhan, and Jawad must remain `GK` (exactly 4 GKs, matching the one-GK-per-team rule). Player data may still receive organizer changes before tournament day.

## Rating and position rules

- One overall rating only; no FIFA-style subratings
- Rizvi is 90 and must remain the uniquely highest-rated player unless explicitly changed
- Current top tier (85–90): Rizvi 90; Navid and Munem 89; Rahmat and Masrur 88; Samin, Minhaz, Jawad, and Tahsin 87; Abir 86; Faiad, Mahtab, and Farhan Labib 85
- Remaining ratings stay within 75–84
- Allowed positions only: `GK`, `DEF`, `MID`, `ATT`
- Position colors: GK purple, DEF blue, MID green, ATT orange

## Auction queue and transaction behavior

- Implemented in Milestone 3 (`services/randomization_service.py` + `models/auction.py`): one shuffled queue containing all 28 eligible players exactly once, captains excluded, optional seed for reproducible tests, `Auction.advance()`/`current_player_id`/`has_current_player`/`is_complete` to reveal one player at a time without exposing the rest of the queue
- Implemented in Milestone 7 (`services/auction_service.py`): `process_sale`/`process_unsold` validate leader/price/budget/roster-space/eligibility/captain-status/prior-status, deduct budget, assign roster, and write `AuctionHistoryEntry` history — see "Completed in Milestone 7" for full detail.
- **Corrected in Milestone 8.1**: an earlier note here said "UNSOLD players do not automatically return to the queue" — that was wrong for the real tournament and has been fixed. UNSOLD players now return in a re-auction round once the current round's queue is exhausted, and the auction only completes when every eligible player is SOLD. See "Completed in Milestone 8.1" for full detail.
- **Implemented in Milestone 9** (`services/persistence_service.py`): autosave after every successful SOLD/UNSOLD (and the round transitions/BLOCKED/COMPLETE they trigger); teams, queue/round position, results, and full history are all restored exactly after a restart. See "Milestone 9 — Persistence / Autosave / Recovery" for full detail.
- Still not implemented (future milestones):
  - Live bidding / bid increments (1M, 2M, 5M) — the transaction service accepts a final sale price supplied directly by the caller, not a live bid ladder

## UI status

- Implemented in Milestone 4: dark sports-tech app shell (`ui/main_window.py`, `ui/theme.py`), green accent, header with the configurable Calgary Vipers logo/title (graceful text fallback if the logo can't load), sidebar navigation with an active-state highlight, and placeholder screens for each route. Live Auction is the default screen on launch. Default window size 1366×768, minimum 1024×640, scales fine at 1920×1080.
- Implemented in Milestone 5: a real, read-only Players & Setup screen (`ui/screens/players_setup_screen.py`) — see "Completed in Milestone 5" for details.
- Implemented in Milestone 6: a real, read-only Teams screen (`ui/screens/teams_screen.py`) — see "Completed in Milestone 6" for details.
- Implemented in Milestone 8: a real, interactive Live Auction screen (`ui/screens/live_auction_screen.py`) — see "Completed in Milestone 8" for details. It's now `MainWindow`'s default screen.
- Implemented in Milestone 9: a session launcher (NEW MOCK/NEW LIVE, resume/delete saved sessions) shown whenever no session is active, plus a MOCK/LIVE mode badge and autosave-status indicator once one is — see "Milestone 9 — Persistence / Autosave / Recovery" for details.
- Implemented September 2026 ("Player Cards + Last-Season FPL"): a seventh sidebar route, **Player Cards** (`ui/screens/player_cards_screen.py`) — a visual gallery of all 32 players, separate from Players & Setup's administrative table. Sidebar order is now: Live Auction, Player Cards, Players & Setup, Teams, Auction History, Reports, Settings.
- Implemented September 2026 ("Auction History — Transaction Timeline + Filtering"): **Auction History** (`ui/screens/auction_history_screen.py`) is now a real, read-only, chronological transaction-timeline screen for the current session — see "Auction History — September 2026" for full detail.
- Implemented September 2026 ("Reports — Auction Summary / Analytics Dashboard"): **Reports** (`ui/screens/reports_screen.py`) is now a real, read-only auction summary/analytics dashboard for the current session — see "Reports — September 2026" for full detail.
- Implemented September 2026 ("Settings — Organizer Controls / App Preferences"): **Settings** (`ui/screens/settings_screen.py`) is now a real organizer configuration/diagnostics screen — see "Settings — September 2026" for full detail. Every sidebar route now has a real screen; no Milestone 4 placeholders remain.
- Still not implemented (future milestones):
  - Live bidding between teams (Milestone 8 accepts one final price per sale, entered by the organizer, not a bid ladder)
  - Player/team database editing/management (add, delete, edit rating/position, change captain or team, base-price editing, manual roster assignment, budget/squad-limit edits, team logo upload)
  - PDF/CSV export of Reports data (deliberately deferred — see "Reports — September 2026")
  - Captain phone bidding (Settings' "Captain Phone Bidding" section is a deliberate future-feature placeholder only — see "Settings — September 2026")

## Current milestone

Milestone 2 = COMPLETE
Milestone 3 = COMPLETE — randomized hidden auction queue / order engine.
Milestone 4 = COMPLETE — basic CustomTkinter app shell / navigation.
Milestone 5 = COMPLETE — Players & Setup screen (read-only).
Milestone 6 = COMPLETE — Teams screen (read-only).
Milestone 7 = COMPLETE — Auction transaction / SOLD + UNSOLD business logic.
Milestone 8 = COMPLETE — Live Auction screen.
Milestone 8.1 = COMPLETE — Re-Auction / GK / Team Eligibility Rules.
Player Data Refresh — September 2026 = COMPLETE — canonical roster/ratings updated (not a numbered milestone).
Player Photo Integration — September 2026 = COMPLETE — real player photos + Live Auction card polish (not a numbered milestone).
Pre-Milestone-9 Budget Reserve Fix = COMPLETE — minimum budget reserve for remaining roster slots (not a numbered milestone).
Milestone 9 = COMPLETE — Persistence / Autosave / Recovery.
Player Cards + Last-Season FPL — September 2026 = COMPLETE — new Player Cards gallery screen + last-season FPL points (not Milestone 10 yet).
Auction History — Transaction Timeline + Filtering — September 2026 = COMPLETE — real, read-only transaction-log screen (not Milestone 10 yet).
Reports — Auction Summary / Analytics Dashboard — September 2026 = COMPLETE — real, read-only analytics dashboard (not Milestone 10 yet).
Settings — Organizer Controls / App Preferences — September 2026 = COMPLETE — real organizer configuration/diagnostics screen (not Milestone 10 yet).
Captain Phone Bidding — Phase 1 — September 2026 = COMPLETE — one real phone can bid for one team over the local network while the organizer bids for the others from the laptop; PIN auth for all 4 captains simultaneously is Phase 2 (not Milestone 10 yet).

## Completed in Milestone 1

- Repository/folder structure
- Player, Team, Auction, and AuctionHistoryEntry models
- Model validation and dictionary serialization
- Full player/team/settings JSON configuration
- Official Calgary Vipers logo asset and configurable branding metadata
- Empty initial auction state
- Core tests for roster counts, captain rules, required positions, rating ranges, budgets, squad limits, duplicate protection, and model serialization
- README and project context
- Follow-up hardening: `Player` now rejects `PRE_ASSIGNED` status on any non-captain; added tests cross-checking `settings.json` counts against the canonical player/team data and verifying team-captain links

## Completed in Milestone 2

- Verified all 32 player records in `data/players.json` are final: unique contiguous ids 1–32, unique full and short names, valid positions, no duplicate or missing data
- Confirmed exactly 4 captains pre-assigned with `auction_eligible = false` and status `PRE_ASSIGNED`: Samin Haque → Blackout FC, Sabit Khan → Darkstar FC, Arafatul Mamur → Goli Underdogs, Riyad Zaman → Showstoppers
- Confirmed exactly 28 auction-eligible players
- Confirmed all four teams initialize with a 100M budget, their captain already in a 1-player roster, and zero auction spending/purchases
- Confirmed required positions (Nabil/Masrur/Rayhan/Jawad = GK) and ratings against the canonical data of the time (superseded by the September 2026 player-data refresh — see "Player Data Refresh — September 2026")
- Confirmed no player carries a permanent `base_price`, `sold_price`, or auction result yet
- Confirmed Calgary Vipers remains branding-only and is not one of the four auction teams
- No data file changes were required — the Milestone 1 data was already correct; added tests to lock in these guarantees going forward

## Completed in Milestone 3

- `services/randomization_service.py`: `create_auction_queue(players, seed=None, expected_count=28)` builds one randomized queue of auction-eligible player ids
- Filters strictly on `auction_eligible`; independently re-verifies no captain slipped into that pool (defense-in-depth against a corrupted in-memory `Player`, since `is_captain`/`auction_eligible` are plain mutable dataclass fields, not enforced continuously)
- Rejects duplicate ids in the input collection, and rejects an eligible-player count that does not match `expected_count` (default 28, matching `settings.json`'s `auction_player_count`); `expected_count=None` skips that check for non-standard tournament sizes
- Uses a local `random.Random(seed)` instance (never the global `random` module) so the same players + seed always reproduce the same order, while omitting the seed uses ordinary randomness; the seed is stored on `Auction.random_seed` purely for reproducibility/debugging and is never required by tournament-mode UI
- `create_auction(players, seed=None)` wraps queue creation and returns a ready `Auction` with `status=IN_PROGRESS`, `current_queue_position=0`
- Never mutates the input player collection or touches `data/players.json` — the queue is a freshly built list of ids
- Extended the existing `Auction` model (not redesigned): added `is_initialized` (queue non-empty), `has_current_player` (a player is currently waiting), and `advance()` (moves `current_queue_position` forward one step, raising `ValueError` on an empty or already-completed queue instead of silently continuing)
- The queue is generated once per auction; nothing re-shuffles or re-samples from the full player database after a player is processed — `advance()` only ever moves forward through the queue that was built at auction start

## Completed in Milestone 4

- `main.py` is now the minimal real entry point: it just calls `ui.run()`, which builds `MainWindow` and starts `mainloop()`. No GUI code lives in `main.py` itself.
- `services/config_service.py`: `load_branding_config()` and `load_position_colors()` read `data/settings.json` once and return a typed `BrandingConfig` / plain dict, so UI code never parses JSON or hard-codes branding strings.
- `ui/theme.py`: dark sports-tech color palette (near-black background, green accent), a `POSITION_COLOR_HEX` mapping from settings.json's semantic position-color *names* (purple/blue/green/orange) to concrete hex values for later screens, window size constants (`1366x768` default, `1024x640` minimum), and small font helpers.
- `ui/main_window.py`: `MainWindow(ctk.CTk)` — a single top-level window with a header (logo + header title + app name), a left sidebar with the six required nav buttons (Live Auction, Players & Setup, Teams, Auction History, Reports, Settings), and a central content area. `show_screen(name)` destroys the previous placeholder frame and builds a new one in-place (no new windows, no overlapping frames) and updates the active button's styling. Defaults to "Live Auction" on launch.
- Logo loading (`_load_logo_image`) opens the branding logo with Pillow, calls `.load()` to force-read it immediately, and catches `FileNotFoundError`/`OSError`/`UnidentifiedImageError` to return `None` instead of raising; `MainWindow` falls back to a small "CV" text badge when that happens, so a missing or corrupt logo file cannot crash the app. Verified manually with both a missing-file path and a corrupted-file path (screenshots), and with unit tests.
- Placeholder screens for all six routes show only an uppercase title, a one-line description, and "Coming in a later milestone" — no auction functionality implemented.
- Manually verified by launching `python main.py` and by scripted screenshot checks: header/logo render, sidebar renders, Live Auction shows by default, clicking through to another screen swaps content cleanly with the active button highlight moving, and the missing-logo fallback badge displays correctly.
- New tests (`tests/test_ui_shell.py`) check config loading, module imports, and the logo-loader's missing/corrupt-file fallback unconditionally; a `MainWindow` creation/navigation test runs only when a real display/Tk backend is available and is skipped (not failed) otherwise.

## Completed in Milestone 5

- Replaced only the "Players & Setup" placeholder with a real, **read-only** screen; the other five sidebar routes are untouched Milestone 4 placeholders. Routing is a small `ui/screens/SCREEN_BUILDERS` dict keyed by nav name, checked by `MainWindow.show_screen()` before falling back to the generic placeholder — no per-screen `if/elif` chain in `MainWindow`, and no auction/roster logic added there.
- `services/player_service.py`: `load_players()` / `load_teams()` read `data/players.json` / `data/teams.json` in canonical on-disk order (never randomized, never written back to); `filter_players(players, search, position, status)` does a case-insensitive, partial-match search against full and short names plus optional position/status filters, always preserving canonical order; `validate_setup(players, teams)` runs the tournament invariant checks below and returns a `SetupValidationResult` (a list of pass/fail `SetupCheck`s) instead of raising, so a normal data problem renders as a readable status rather than crashing.
- Setup validation checks: total players (32), captain count (4), auction-eligible count (28), unique player ids, no captain marked auction-eligible, team count (4), and — cross-referencing `data/teams.json` — that each team's `captain_player_id` resolves to a player who is marked `is_captain`, whose name matches `captain_name`, and whose `assigned_team` matches the team's `name`. The expected counts (32/4/28/4) are plain module constants mirroring `data/settings.json`, following the same pattern as Milestone 3's `EXPECTED_ELIGIBLE_PLAYER_COUNT` — overridable per call, no file I/O inside `validate_setup` itself. On the canonical data this reports **Tournament Setup Ready**.
- `ui/screens/players_setup_screen.py`: `PlayersSetupScreen(ctk.CTkFrame)` renders a title/description, six summary stat cards (Total Players, Auction Players, Captains, Teams, Budget/Team, Squad Limit — computed directly from the loaded players/teams, not re-parsed from settings), the green/red setup-status banner, a search box + position segmented filter (All/GK/DEF/MID/ATT) + status segmented filter (All Players/Auction Players/Captains), and a scrollable 32-row table (ID, Player, Short Name, Position badge, OVR, Status badge, Assigned Team). Position badge colors come from `services/config_service.load_position_colors()` mapped through `ui/theme.POSITION_COLOR_HEX` — no color values are duplicated in the screen module. The table always starts in canonical `data/players.json` order; filtering narrows that same ordered list, never reorders it.
- Search re-filters live via a `StringVar` write-trace; the position/status segmented buttons re-filter on their `command` callback. All three filters compose (AND semantics) through one call to `filter_players()`; clearing search back to empty and both filters back to "All"/"All Players" restores all 32 players in canonical order.
- Read-only by design: no add/delete/edit/photo-upload/base-price controls exist. Opening the screen never mutates `data/players.json` or `data/teams.json`.
- Error handling: `PlayersSetupScreen.__init__` catches `OSError`, `json.JSONDecodeError`, `ValueError`, `KeyError`, and `TypeError` around loading players/teams/position-colors (covering a missing file, malformed JSON, and an invalid record/captain-team relationship that `Player`/`Team` validation rejects) and renders a readable in-screen error message instead of propagating — no broad `except Exception`.
- Manually verified via `python main.py` and scripted screenshots: the default 32-row table, the search+position-filter example from the spec ("rah" + GK → only Masrur Rahman), the "arik" search example (→ only Shahriar Anwar Khan), and confirmed the other five sidebar routes still render as plain Milestone 4 placeholders.
- Deferred to later milestones: player photos, the large Live Auction player card, and any editing/management controls (add/delete player, edit rating/position, change captain or assigned team, base-price editing).

## Completed in Milestone 6

- Replaced only the "Teams" placeholder with a real, **read-only** screen (registered in `ui/screens/SCREEN_BUILDERS`, same routing pattern as Milestone 5); the other four sidebar routes (Live Auction, Auction History, Reports, Settings) are still Milestone 4 placeholders, and Players & Setup is unchanged.
- `services/team_service.py`: `TeamSummary` (a `Team` plus its roster resolved to `Player` objects) with `squad_size`, `remaining_slots`, `total_spent`, and `remaining_budget` derived as properties directly from the `Team` model — nothing is recomputed independently or hardcoded. `build_team_summary(team, players_by_id)` resolves one team's `roster` ids to `Player` objects; any id with no match is reported in `unresolved_roster_ids` instead of raising, so a corrupted roster degrades to a readable partial state. `build_team_summaries()` does this for every team, preserving team order; `load_team_summaries()` chains `player_service.load_players()` / `load_teams()` with resolution in one call. No write/update methods exist.
- `ui/screens/teams_screen.py`: `TeamsScreen(ctk.CTkFrame)` renders a title/description, four summary stat cards (Teams, Starting Budget/Team, Squad Limit, Total Auction Budget — computed from the loaded teams, not re-parsed from settings), and a scrollable 2×2 grid of team cards (`Blackout FC`, `Darkstar FC`, `Goli Underdogs`, `Showstoppers`, in that canonical order). Each card shows the captain (name + green CAPTAIN badge, or a red "could not be resolved" message if the captain id doesn't resolve), five mini budget/squad stats (Remaining Budget, Starting Budget, Total Spent, Squad `n / 8`, Remaining Slots), and a ROSTER section listing every resolved roster player (name, position badge, CAPTAIN badge if applicable) — built from `team.roster` in order, not hardcoded to the captain alone, so it already supports up to 8 members for when Live Auction starts adding purchased players.
- Extracted `ui/widgets.py` (`pill_badge`, `position_badge`, `captain_badge`, `auction_status_badge`) out of Milestone 5's `players_setup_screen.py` so both screens share one implementation of the colored badge widgets instead of duplicating the position-color lookup and styling; `players_setup_screen.py` was refactored to use it with no behavior change (its full test suite still passes unchanged).
- On the canonical data every card shows exactly what the milestone specifies: Squad `1 / 8`, `7` remaining slots, `0M` total spent, `100M` remaining budget, captain in the roster.
- Error handling mirrors Milestone 5's pattern: `TeamsScreen.__init__` catches `OSError`, `json.JSONDecodeError`, `ValueError`, `KeyError`, `TypeError` around loading and renders a readable in-screen error message instead of propagating; an unresolved captain or roster id renders inline (red text / a flagged roster row) rather than crashing the screen.
- Manually verified via `python main.py` and a scripted screenshot: all four team cards render with correct captain, budget, and squad figures; Players & Setup still renders its own screen; Live Auction/Auction History/Reports/Settings remain plain placeholders.
- Deferred to later milestones, as instructed: any write/update actions (manual assignment, removal, transfer, captain/budget/squad-limit edits, team logo upload), and team logos themselves (no assets exist yet — cards use clean text headers only).

## Completed in Milestone 7

- `services/auction_service.py`: `process_sale(auction, players, teams, winning_team, sale_price)` and `process_unsold(auction, players, teams)` — the first code allowed to mutate `Team`/`Player`/`Auction` state together. No GUI or `main.py` code performs this orchestration; both functions operate purely in memory and never touch `data/players.json` or `data/teams.json`.
- **Validation-before-mutation, not try/rollback**: every legality check (auction initialized and not complete; current player resolves, is not a captain, is auction-eligible, and has not already been resolved; for SOLD — winning team exists, price is a positive `int` (not `bool`/`float`/`≤0`), price ≤ `team.remaining_budget`, roster has room, player isn't already on *any* team's roster) runs before a single object is touched. A failed validation raises `AuctionTransactionError` immediately, so the auction, every team, and every player are left byte-for-byte unchanged — verified by dedicated atomicity tests (`copy.deepcopy` snapshots compared after each rejected transaction).
- SOLD reuses `Team.add_purchased_player()` for the actual budget/roster mutation (no duplicated budget math) after all pre-checks already guarantee it will succeed; UNSOLD never calls it and never touches any `Team`.
- Both outcomes update the resolved `Player` object directly: `auction_status` → `SOLD`/`UNSOLD`, `auction_sequence` → the transaction's 1-based sequence number (shared with the history entry). SOLD additionally sets `sold_price` and `sold_to` (the team name); UNSOLD leaves both `None`. `auction_eligible` is never changed — it is a fixed "was in the original 28-player pool" category flag (set once at Milestone 2/3), not a live status; `auction_status` is what actually tracks progress through the auction.
- History uses the existing `AuctionHistoryEntry` model unchanged — no second/competing history representation. `resolved_player_ids(auction)` (`{entry.player_id for entry in auction.history}`) is the single reusable way to check whether a player has already been resolved; both `process_sale` and `process_unsold` use it, and it is exported for the future Live Auction screen to use directly (e.g. to gray out resolved players in a history view).
- Each successful transaction calls `Auction.advance()` exactly once, only after all mutation has already succeeded; a failed transaction never calls it. When `advance()` leaves `Auction.is_complete` true, `auction.status` is synced to `AuctionStatus.COMPLETE` (reusing the existing enum — no second completion flag). Once complete, both functions raise `AuctionTransactionError("Auction is already complete.")` immediately rather than allowing further transactions.
- `get_current_player(auction, players)` resolves `Auction.current_player_id` to a `Player` (or `None`), for callers (like the future Live Auction screen) that just want to display who's up without performing a transaction.
- `TransactionResult` (returned by both functions): `player`, `outcome` ("SOLD"/"UNSOLD"), `team` (`None` for UNSOLD), `sale_price` (`None` for UNSOLD), `history_entry`, `auction_complete`, `next_player_id` — designed so Milestone 8's Live Auction screen can render a result and immediately know what to show next without re-deriving state.
- Verified with a full run through all 28 queued players (round-robin sale across the 4 teams at 1M each) ending with every team at exactly 8/8 roster and 7/7 purchases, `auction.status == COMPLETE`, and `len(auction.history) == 28` — confirming the queue/completion/history bookkeeping holds up over a full auction, not just single transactions.
- Players & Setup and Teams screens were not touched and continue to import and load data correctly (re-verified by test and by re-running their existing test suites).
- Deferred, as instructed: no persistence/autosave (transactions are in-memory only — re-running the app loses all transaction state), no Live Auction GUI (still the Milestone 4 placeholder), no bidding/increment logic, no reports.

## Completed in Milestone 8

- `services/auction_session_service.py`: `AuctionSession` — the single shared, in-memory holder of the running `Auction`, `players`, and `teams` for the life of the application process. `MainWindow` owns exactly one instance (`self.session`, injectable for tests) and passes it to every screen builder via `SCREEN_BUILDERS[name](parent, session)`, so navigating Live Auction → Teams → Live Auction never touches the session object — it survives screen destroy/recreate because `MainWindow`, not the screen, owns it. `session.start(seed=None)` is a **no-op if already started** (not an error) — this is the actual mechanism that guarantees the queue is never re-randomized by a stray extra call, not just a UI convention. `sell_current_player()`/`mark_current_player_unsold()` thin-wrap `services/auction_service.process_sale`/`process_unsold` (no business rules re-implemented) and record `last_result`; `current_player`/`resolved_count`/`total_queue_length`/`remaining_count`/`is_complete` are read-only derived properties for the UI's progress display — none of them expose the raw upcoming queue, so future player identities can't leak through this API.
- `ui/screens/live_auction_screen.py`: `LiveAuctionScreen(ctk.CTkFrame)` replaces the Live Auction placeholder and is now `MainWindow`'s default screen on launch. Internally a small state machine keyed off the shared session: **Not Started** (session summary stats + START AUCTION), **In Progress** (current-player card, 4-team status/selection row, price entry + quick +1M/+2M/+5M buttons, SOLD/UNSOLD actions), **Complete** (final sold/unsold counts + every team's final budget, no further actions possible). Every state transition calls one `_render()` that clears and rebuilds the screen's children from current session state — simple and correct over incrementally patching widgets, and cheap enough at 28 transactions/auction.
- SOLD/UNSOLD both go through a lightweight `CTkToplevel` confirmation dialog (Confirm Sale: player/team/price; Confirm UNSOLD: player) centered over the main window, so an accidental click can't commit a transaction. The dialog is a thin shell — `Confirm` calls `_execute_sale`/`_execute_unsold` directly, which is what actually calls the session and handles success/failure; tests exercise these methods directly rather than opening a real dialog, per the milestone's own testing guidance.
- The GUI never mutates a `Team`/`Player`/`Auction` object itself and never re-implements a validation rule — `_execute_sale`/`_execute_unsold` only call the session, catch `AuctionTransactionError`, and either re-render on success or set a readable `_error_message` and re-render on failure. Usability-level checks (no team selected, empty/non-numeric/non-positive price) are caught in the UI *before* calling the service, with their own readable messages, so an organizer gets immediate feedback without provoking a service-layer exception for an obviously incomplete form.
- Player photos: `Player.photo_path` already existed from Milestone 1 (at the time, `null` for all 32 canonical players — no assets had been added yet). `ui/widgets.load_image_safely(path, max_size)` is a shared helper (`main_window._load_logo_image` was refactored to call it too, no behavior change) that loads an image, preserves aspect ratio, and returns `None` for a missing path, missing file, or corrupt file — the player card falls back to a two-letter initials badge in that case. Never crashes; never fakes a photo. **Superseded by the September 2026 photo integration** — see "Player Photo Integration — September 2026" below: all 32 players now have a real photo, and the Live Auction card uses a different helper (`load_cover_fit_image`) for a consistent crop/fill rather than `load_image_safely`'s shrink-to-fit.
- Team status/selection is one widget cluster, not two: each of the 4 team buttons shows name, remaining budget, squad `n / 8`, and remaining slots, and doubles as the "select winning team" control (accent-green highlight = selected). Only one team can be selected at a time; Calgary Vipers is not and cannot be one of the 4 buttons (they come directly from `session.teams`, the canonical 4-team list).
- Progress ("Player N of 28 · X resolved · Y remaining") and all team figures are derived from `session`/`Team` properties every render — nothing is hardcoded, and the future queue's player identities are never displayed (only the current player, resolved via `AuctionSession.current_player`).
- **Teams screen is now session-aware** (`ui/screens/teams_screen.py`, `build_team_summaries` reused from Milestone 6 rather than duplicated): before a session has started, it shows canonical `data/teams.json` (unchanged Milestone 6 behavior); once a session is active, it builds summaries directly from `session.teams`/`session.players` — the same live, mutated `Team` objects Live Auction just updated — so a purchase is visible on Teams immediately, without ever re-reading canonical JSON (which would have silently discarded the purchase). Players & Setup was intentionally left untouched — it remains a canonical, session-free inspection screen.
- Verified end-to-end with a scripted walkthrough matching this milestone's manual checklist exactly: Not Started → START AUCTION → reveal player → select Blackout FC → price 10 → SOLD (confirmed) → budget 90M/squad 2/8 verified → next player revealed → UNSOLD (confirmed) → other budgets unchanged verified → navigate to Teams (live 90M/2-of-8/roster contains the purchased player, confirmed) → navigate back to Live Auction (still on player 3, `resolved_count == 2`, same session, confirmed). Also verified a full 28-player run reaches `AuctionStatus.COMPLETE` with every team at exactly 93M (7 × 1M spent) and the SOLD/UNSOLD action buttons absent from the rendered widget tree.
- In-memory only, same as Milestone 7: nothing here writes to `data/players.json` or `data/teams.json`, and closing the application still loses all session progress (the running auction, every budget/roster change, and history). Persistence/autosave/recovery is explicitly Milestone 9.
- **Post-Milestone-8 bug fix**: the SOLD/UNSOLD confirmation dialogs originally used a fixed guessed size (`360x280`/`340x220`) set *before* their content was packed, which could clip the Confirm/Cancel buttons off the bottom on some Windows DPI/font-scaling combinations. Fixed by sizing each dialog from its actual packed content (`winfo_reqwidth`/`winfo_reqheight`, read only after everything — including the buttons — has been added) instead of a fixed guess; both dialogs now share one `_build_confirmation_dialog`/`_finalize_dialog`/`_build_dialog_button_row` implementation. The window's own close (X) button is explicitly bound to a safe `dialog.destroy` (no transaction) via `WM_DELETE_WINDOW`.

## Completed in Milestone 8.1

A full manual mock auction (28 players, some intentionally left unsold) exposed that "28 attempts finished" was being treated as "auction complete" even with players still unsold — incorrect for the real tournament. This milestone corrects that and adds two tournament rules that hadn't been enforced yet (at-most-one-GK-per-team, full-team eligibility reflected proactively) plus detection for auctions that become impossible to finish.

- **Re-auction rounds** (`models/auction.py` + `services/auction_service.py`): `Auction` gained `round_number` (starts at 1) and `start_new_round(new_queue)`, which replaces the current (exhausted) round's queue with a freshly shuffled pool and resets `current_queue_position` to 0 — the shuffle itself still lives in the service, not the model, matching the existing "randomization is a service concern" decision. `AuctionStatus` gained `BLOCKED` alongside the existing `NOT_STARTED`/`IN_PROGRESS`/`COMPLETE`.
- **SOLD is the only permanent outcome now.** `sold_player_ids(auction)` (SOLD entries only) replaced `resolved_player_ids(auction)` (all entries, SOLD or UNSOLD — kept, but no longer used for this check) as the "cannot be auctioned again" test inside `_validate_current_player`. `eligible_for_reauction(auction, players)` is every auction-eligible player not yet in `sold_player_ids` — exactly the next round's pool, computed fresh at each round boundary rather than tracked incrementally.
- **Round transition logic** lives in a rewritten `_finish_transaction(auction, players, teams)`, called by both `process_sale` and `process_unsold` after their own mutation succeeds: `advance()` the current round; if the round isn't exhausted, nothing else happens; if it is and `eligible_for_reauction` is empty, `status` becomes `COMPLETE`; if players remain, `team_can_bid_for_player` checks whether *any* pending player could go to *any* team — if yes, a new round starts (queue reshuffled with a seed derived from `random_seed + round_number` for reproducibility, or unseeded if the auction itself was unseeded); if no, `status` becomes `BLOCKED` instead of starting a round that could never resolve, so nothing loops forever. `TransactionResult` gained an `auction_status: AuctionStatus` field; `auction_complete` is now simply `auction_status == COMPLETE` rather than "the round's queue happened to run out."
- **One-goalkeeper-per-team rule**: `team_has_goalkeeper(team, players)` resolves a team's *entire* roster (captain included) to check for a `Position.GK`; `process_sale` rejects a GK sale to a team that already has one ("Team already has a goalkeeper."), checked after the existing budget/roster-space checks and before the duplicate-assignment check (reordered slightly from the original Milestone 7 order — "already assigned to a team" is the more fundamental integrity problem and now takes priority when both are somehow true at once). `team_can_bid_for_player(team, player, players)` combines the roster-space and GK checks into one boolean, ignoring price (the price isn't known ahead of a UI selection) — used by the UI to disable ineligible teams and by the blocked-state check; it does not replace `process_sale`'s own validation, which stays authoritative.
- **GK setup validation** (`services/player_service.validate_setup`): a new "Sufficient goalkeeper coverage" check compares the number of auction-eligible GKs against the number of teams whose captain is *not* already a GK (that team's GK slot is already filled). On the canonical data this is 4 vs 4 — an exact match — so the check passes; it uses `>=` rather than exact equality, since a genuine GK surplus is a runtime concern the BLOCKED-state detection above already covers, not something worth blocking setup over.
- **Live Auction UI**: the round label now reads "Round 1" or "Re-Auction Round N", and progress ("X resolved this round / Y remaining this round") is scoped to the *current round's* queue, not a running total across rounds (the underlying `AuctionSession.resolved_count`/`total_queue_length`/`remaining_count` properties were redefined to read `current_queue_position`/`len(queue)` — which are already round-scoped since `start_new_round` resets them — instead of the old `len(history)`, which grows across rounds and would otherwise show nonsensical numbers like "30 resolved" for a 28-player tournament). Each of the 4 team buttons is now `state="disabled"` (grey, unselectable, no command) whenever `team_can_bid_for_player` says no for the current player, with a "FULL" or "HAS GK" label explaining why; when the current player is a GK, every team button also shows "GK 0/1"/"GK 1/1". If the previously-selected team becomes ineligible before the organizer acts (e.g. it just filled up), the selection is cleared automatically on the next render. A new **AUCTION BLOCKED** view (red heading, the exact message "Re-auction cannot continue: no eligible team can purchase the remaining player(s).", sold-so-far count, and final team states) renders whenever `session.is_blocked` is true, with no SOLD/UNSOLD controls — mirroring the existing Not-Started/In-Progress/Complete state-machine pattern in `_render()`.
- **Completion view** now shows "Sold" and "Rounds" (how many re-auction passes it took) instead of "Sold"/"Unsold" — at true completion "Unsold" is always 0 by definition, so it wasn't a useful stat; "Rounds" is.
- Verified end-to-end with a scripted walkthrough matching this milestone's own example exactly: round 1 (28 players) with 2 deliberately left unsold → confirmed the app does **not** show Auction Complete, and Re-Auction Round 2 starts automatically with a 2-player queue containing only those two → sold one, marked the other unsold again → confirmed Round 3 starts with only that one remaining player → sold it → confirmed **only now** does AUCTION COMPLETE appear (28/28 sold, 3 rounds). Also verified, via screenshots, that a full team correctly shows "FULL" and is disabled, and that when a GK is up for auction, teams already holding a GK show "GK 1/1" and are disabled while a team with room and no GK shows "GK 0/1" and stays selectable. Separately confirmed at the service layer (bypassing the UI entirely) that attempting to sell a second GK to a team that already has one is rejected with "Team already has a goalkeeper." and changes nothing.
- Two Milestone 7 tests were updated because they encoded the old, now-incorrect "reaching the end of the queue means the auction is complete" assumption (`test_final_sold_marks_auction_complete`, `test_final_unsold_marks_auction_complete` — the latter renamed to `test_final_unsold_in_a_round_triggers_reauction_not_completion` since "final UNSOLD completes the auction" is no longer a true statement); two more were updated because they injected an UNSOLD history entry to test "already resolved" and that no longer blocks a resale (`test_already_resolved_player_cannot_be_sold`/`_marked_unsold_again`, both switched to injecting SOLD instead, with new companion tests confirming UNSOLD specifically does *not* block). All other Milestone 7/8 tests were kept and still pass unchanged, other than tests whose naive round-robin team assignment could legitimately hit a new GK conflict — those were updated to pick a team via `team_can_bid_for_player` instead, the same way the UI now does.
- Still in-memory only, same as Milestones 7–8: no re-auction/GK/eligibility state is written to `data/players.json` or `data/teams.json`, and none of it survives an app restart.

## Player Data Refresh — September 2026

A controlled canonical tournament-data refresh (not a numbered milestone — no auction rules, GK rules, team-eligibility rules, budgets, squad limits, team names, Live Auction architecture, persistence, UI layout, or theme were touched). Only `data/players.json` changed.

- Canonical participant list updated to the organizer-approved final-ish roster of 32 players (4 captains + 28 auction-eligible), using the exact ids/order supplied by the organizer.
- **Samin Haque changed position MID → ATT** (rating also updated to 87); remains captain of Blackout FC at zero cost, `PRE_ASSIGNED`, not auction-eligible.
- **Removed**: Aldeen, Mirza, Sarim — no longer present anywhere in `data/players.json`, canonical documentation, or current-data tests. (Their names may still appear in this file's own historical milestone notes above, which is expected — those describe what was true at the time, not current data.)
- **Munem relocated and rerated**: now id 24, `MID`, rating 89 — appears exactly once; the previous id-31 "Munem" (ATT, placeholder rating) no longer exists as a separate record.
- **Added**: Hasnan Siddique Sunve (id 30, DEF, 82), Farhan Mahin (id 31, ATT, 78), Tahsin Islam (id 32, ATT, 87) — filling the ids vacated by Mirza, the old Munem slot, and Sarim respectively.
- **Sajid Khalid** rating explicitly organizer-approved as 75 (previously an editable placeholder).
- Ratings refreshed using last season's performance points converted to a 75–90 OVR tier system for returning players, plus explicit organizer-approved ratings for players without usable prior-season points (Hussain Yeasin, Masrur Rahman, Rayhan, Jawad, Farhan Labib, Munem, Hasnan Siddique Sunve, Farhan Mahin, Tahsin Islam) and for Sajid Khalid. No rating was recalculated, reinterpreted, or smoothed — every value came directly from the organizer-supplied list. Ratings are no longer marked as editable placeholders (the `*` convention used before this refresh is retired), though the organizer may still revise them before tournament day.
- **Exactly 4 captains / 28 auction-eligible players maintained** — the refresh only changed identities, positions, and ratings within the existing 32-slot structure; captain count, auction-eligible count, and all four captain-team mappings (Blackout FC/Samin Haque, Darkstar FC/Sabit Khan, Goli Underdogs/Arafatul Mamur, Showstoppers/Riyad Zaman) are unchanged.
- **Exactly 4 goalkeepers maintained**: Nabil Shahriar, Masrur Rahman, Rayhan, Jawad — same four players as before the refresh (only their ratings changed), so the Milestone 8.1 one-GK-per-team rule and its setup-time GK-coverage check (`validate_setup`) continue to pass on the refreshed data without any code change.
- `data/teams.json` was not modified — captain ids/names still resolve correctly against the refreshed `data/players.json`, so no structural drift was introduced.
- Verified end-to-end after the data change: `python -m pytest -v` (344 passed, 2 skipped — same 2 pre-existing environmental skips as Milestone 8.1, 0 failed), `validate_setup()` still reports Tournament Setup Ready (including goalkeeper coverage), a fresh seeded `AuctionSession` still produces a 28-player Round 1 queue with zero captains and all three new players eligible, and the Players & Setup screen was visually confirmed (via `python main.py`) to display the new canonical order, ratings, and positions correctly with Aldeen/Mirza/Sarim absent.
- New `tests/test_player_data_refresh.py` locks in the refreshed roster directly (id/position/rating for every explicitly-specified player, removed-player absence, uniqueness, GK count, captain mappings, queue/GK-coverage compatibility) so a future accidental data edit is caught immediately; `tests/test_player.py`'s position/rating tests were updated in place since they encoded the pre-refresh roster (old GK-adjacent MID/ATT names, old rating tiers) — no test coverage was weakened, only re-pointed at the new correct values.

## Player Photo Integration — September 2026

A focused photo-integration and Live-Auction-card polish pass (not a numbered milestone — no tournament business logic, ratings, positions, team data, re-auction/GK/full-team rules, or persistence were touched). `data/players.json` changed only in its `photo_path` values; the Live Auction and Players & Setup screens gained photo rendering.

- **All 32 canonical players now have a real photo asset**, sourced from `assets/players/` (filenames standardized as `snake_case(full_name).jpg`, e.g. `samin_haque.jpg`). `data/players.json`'s `photo_path` field is populated for every player (captains included, even though they're never auctioned) with an explicit relative path (e.g. `"assets/players/samin_haque.jpg"`) rather than derived dynamically at render time — an explicit stored path survives a future name change, a future filename change, or (later) a persisted session snapshot without extra lookup logic.
- **New reusable image helper**: `ui/widgets.load_cover_fit_image(path, display_size, top_bias=0.25)`. Unlike the existing `load_image_safely` (shrink-to-fit within a box, leaving empty space for a mismatched aspect ratio — still used unchanged for the branding logo), this crops so the result exactly fills `display_size` with no stretching and no letterboxing, which is what a fixed-size player card needs given the real photos arrive in wildly different resolutions and aspect ratios (square headshots, tall portraits, some near-landscape crops). The crop is centered on the dimension that doesn't need cropping and, on the dimension that does, biased toward the top (`top_bias < 0.5`) when cropping height, so a portrait subject's face survives rather than a plain center-crop risking cutting it off. Mirrors `load_image_safely`'s never-raise contract: a missing, unreadable, or corrupt file returns `None` so the caller falls back to a placeholder instead of crashing app startup, Players & Setup, or Live Auction.
- **Live Auction player card upgraded** (`ui/screens/live_auction_screen.py`): redesigned as a tall, centered, sports-broadcast-style card showing only the four essential fields — photo (largest element, 232×290 4:5 portrait via `load_cover_fit_image`), full name (large, uppercase), position badge (existing configured position color), and OVR (large, accent green) — deliberately nothing else, per the project's existing "essential auction information only" rule. The photo falls back to the existing two-letter initials badge (same size) when `photo_path` is `None` or the file can't be loaded. Readable at both 1366×768 and 1920×1080 without clipping the team-status/controls/action rows beneath it (verified manually at the default size).
- **Players & Setup gained a small thumbnail column** (`ui/screens/players_setup_screen.py`): a compact 32×40 `load_cover_fit_image` thumbnail (or a single-letter placeholder) as the second table column, reusing the same helper rather than a second implementation. Kept intentionally small — the screen's read-only, searchable/filterable table structure was not otherwise redesigned.
- **Teams screen was not touched**, as instructed — it does not yet show roster player photos.
- **Tk image garbage-collection safety**: `CTkImage`/`PhotoImage` objects are dropped (and the label goes blank) the instant nothing else references them. Live Auction already kept its single current-player image alive via `self._current_photo_image` (Milestone 8); Players & Setup — which renders up to 32 photos at once in a scrollable table — now does the same via `self._row_photo_images: list`, cleared and repopulated at the start of every `_refresh_table()` call so a stale reference from a previous filter/search pass doesn't linger.
- Verified end-to-end: `python -m pytest -v` (375 passed, 2 skipped — the same 2 pre-existing environmental skips as prior milestones, 0 failed) and a scripted manual walkthrough of `python main.py` — Samin Haque, Rizvi Ibrahim, Taqi Rahman, Tahsin Islam, Masrur Rahman, and Farhan Labib were each forced to the current-player slot and screenshot-confirmed showing the correct photo (across square/portrait/close-up/full-body source shapes) with no distortion, correct name/position/OVR, and no layout clipping; a real SOLD transaction (with its confirmation dialog) was also exercised end-to-end and confirmed the budget/roster update and the next player's photo loading correctly afterward.
- New `tests/test_player_photos.py` covers: every canonical player has a non-null, unique `photo_path` under `assets/players/` with a supported extension whose file actually exists; `load_cover_fit_image` returns `None` (not a crash) for a missing or corrupt file, loads real images of varied source shapes, and produces the exact requested display size for every one of the 32 real canonical photos; and that ratings/positions/captain assignments were not disturbed by the `photo_path` edit. `tests/test_players_setup_screen.py` and `tests/test_live_auction_screen.py` gained tests for the new thumbnail column and the real-photo/placeholder-fallback paths respectively; one Milestone 8 test that asserted `photo_path is None` on real canonical data was updated (it no longer holds now that every player has a photo) to instead test the fallback branch directly via a synthetic `None` override — no coverage was removed, only re-targeted.
- Persistence/autosave remains not implemented — nothing here writes photo state to disk beyond the already-existing `data/players.json` edit, and it remains the next major (still-unscoped) milestone.

## PRE-M9 BUDGET RESERVE RULE

A full manual mock auction exposed a real tournament-business-logic bug: a team could spend so much on one purchase that it became mathematically impossible to fill its remaining roster slots afterward (e.g. a team needing 3 more players with only 3M left could still be offered — and could still accept — a 2M or 3M purchase, leaving too little for the other 1-2 slots at the 1M minimum). This is a focused fix for that gap, completed before Milestone 9 (persistence/autosave) begins. Not a numbered milestone — no auction rules, ratings, positions, team data, GK rules, full-team rules, blocked-state semantics, player photos, or persistence status were otherwise touched.

- **Minimum legal price**: `MINIMUM_LEGAL_PRICE = 1` (1M), defined once in `models/team.py` and imported everywhere else that needs it (`services/auction_service.py`) rather than repeated as a magic number. `process_sale`'s existing price validation (`sale_price` must be a positive integer) already enforced this threshold before this fix; the fix only makes the constant explicit and shared rather than changing that check's behavior.
- **The reserve rule**: for any potential purchase, `remaining_slots_after_purchase = (max_squad_size - roster_size) - 1`, and `required_reserve_after_purchase = remaining_slots_after_purchase * MINIMUM_LEGAL_PRICE`. A sale is legal only if `budget_after_purchase >= required_reserve_after_purchase` — equivalently, `price <= current_budget - required_reserve_after_purchase`.
- **`Team.maximum_legal_bid`** (`models/team.py`, a property computed purely from the team's own `roster_size`/`max_squad_size`/`remaining_budget` — no `Player` data needed) implements that formula directly: the most a team could legally pay for its very next purchase while still able to afford every purchase after that at the minimum legal price. Worked examples (all verified by `tests/test_budget_reserve_rule.py`): fresh team (1/8, 100M) → max first-purchase price 94M (reserving 6M for the 6 slots still needed after that purchase); roster 5/8 with 3M left → max 1M; roster 6/8 with 5M left → max 4M; roster 7/8 (last slot) with 10M left → max 10M, since no reserve is needed after the final purchase.
- **`Team.can_afford_while_preserving_roster_budget(price)`** combines the minimum-price and maximum-legal-bid checks into one boolean, and **`Team.can_buy_player()`** (the model's own existing low-level invariant, already checked by `Team.add_purchased_player()`) now calls it alongside its prior checks — so even a caller that bypassed the service layer's own validation would still be rejected at the model layer. `Team.can_afford()` (the older, simpler "non-negative and within remaining_budget" check) is unchanged and still used elsewhere; the new check is strictly stronger (`maximum_legal_bid <= remaining_budget` always), not a replacement.
- **Service-layer enforcement** (`services/auction_service.py`, before mutation, same "validate everything, then mutate" pattern as every other `process_sale` check): the previous plain `price > team.remaining_budget` check was replaced with `price > team.maximum_legal_bid`, raising a new `budget_reserve_violation_message(team)` — a single shared helper (not duplicated between the service layer and the UI) that names the team, the reserve amount, the number of roster spots it's for, and the resulting maximum legal price (e.g. "Blackout FC must reserve 2M of its remaining budget for its 2 remaining roster spots. Maximum legal price is 1M."), or the simpler "Team does not have enough budget." once no further reserve is needed (the team's very last slot). A rejected sale remains fully atomic — the check runs before any mutation, exactly like the existing GK/roster-full/duplicate-assignment checks it sits alongside.
- **Team eligibility** (`team_can_bid_for_player`, reused unchanged by the UI's team-button disabling and by blocked-state detection — one helper, not duplicated): now also returns `False` when `team.maximum_legal_bid < MINIMUM_LEGAL_PRICE`, i.e. when the team couldn't legally pay even the minimum price for *any* player without breaking its own reserve. This doesn't require knowing the actual price in advance (unlike a specific transaction attempt) since `maximum_legal_bid` depends only on the team's own state. Combined with the pre-existing roster-space and GK checks, a purchase must now satisfy all three independently — the budget rule doesn't override the GK rule and vice versa.
- **Blocked-state integration**: `_finish_transaction`'s "can any pending player legally go to any team" check already calls `team_can_bid_for_player` for every (player, team) pair, so it automatically inherited the new budget-affordability condition with no separate code path — a team that's technically not full and has no GK conflict but can't legally afford even the minimum price is now correctly excluded, and an auction that becomes budget-starved on all sides now reports BLOCKED instead of incorrectly starting an unwinnable re-auction round.
- **Live Auction UI** (`ui/screens/live_auction_screen.py`): each of the 4 team buttons now shows a compact "Max {N}M" line (clamped to 0 for display in a pathological negative case) alongside the existing budget/roster/slots lines, so the organizer can see the legal ceiling before entering a price. An ineligible team's reason label now distinguishes three cases instead of two: "FULL" (roster full), "HAS GK" (goalkeeper conflict for the current player), or "LOW BUDGET" (can't legally afford even the minimum price while preserving its reserve). Entering a price above `team.maximum_legal_bid` is rejected *before* the confirmation dialog even opens (chosen over disabling the SOLD button, consistent with the screen's existing pattern of catching usability-level problems — no team selected, invalid price format — before calling the service layer), showing the exact same `budget_reserve_violation_message` text the service layer would raise, so the wording can never drift between the two surfaces. The price is never silently clamped.
- Verified end-to-end via a scripted `python main.py` walkthrough matching the ticket's exact manual scenario: a team forced (synthetic setup) to 5/8 roster with 3M remaining showed "Max 1M" and rejected a 2M attempt with the reserve message *before* opening the confirmation dialog, with budget/roster fully unchanged afterward; a 1M sale then succeeded, recalculating to 2 slots / 2M / "Max 1M"; repeating this to the final slot left the team at exactly 8/8 with 0M remaining. Separately verified a 7/8-roster team with 10M remaining could legally spend all 10M on its final player.
- Persistence/autosave remains unimplemented and is still the next (unscoped) major milestone — nothing here writes to disk beyond the in-memory `Team`/`Auction` objects any other transaction already mutates.

## Milestone 9 — Persistence / Autosave / Recovery

Everything through the Pre-Milestone-9 budget reserve fix ran entirely in memory: closing the app, a crash, or a restart lost the whole running auction. This milestone adds a full JSON-based persistence layer so a MOCK or LIVE auction survives exactly across a restart, with autosave, atomic writes, and corrupt-save safety — without touching auction rules, ratings, positions, team names, GK rules, or the budget reserve rule.

- **MOCK vs LIVE sessions** (`services/auction_session_service.SessionMode`): every session is explicitly one or the other, stored in the save file as `session_mode`. MOCK is for practice — any number may exist side by side, freely created and deleted. LIVE is the one protected, real-tournament session — always saved to the single `saves/live/live_active.json` slot, never to a mocks file and vice versa (structurally guaranteed by `persistence_service.session_save_path`, which is the *only* thing that decides where a save goes and switches purely on `session.mode`, which never changes after a session is created).
- **Save directory** (`services/persistence_service.py`, alongside `data/` and `assets/` under the existing `ROOT_DIR` pattern from `services/config_service.py` — packaging-friendly, no reliance on the current working directory): `saves/mocks/<session_id>.json` per practice session, `saves/live/live_active.json` for the current tournament session, and `saves/live/live_archived_<id>.json` for a previous LIVE session preserved (never deleted) when a new one replaces it. `saves/` is git-ignored (real tournament data must never be committed) and created on demand — nothing pre-creates it.
- **Session snapshot content**: a save file is `{schema_version, session_id, session_mode, name, created_at, updated_at, players: [...], teams: [...], auction: {...}}`, reusing `Player.to_dict()`/`Team.to_dict()`/`Auction.to_dict()` (and their `from_dict()` counterparts) unchanged rather than inventing a second serialization format — a `Player`'s `auction_status`/`sold_price`/`sold_to` and a `Team`'s `roster`/`remaining_budget`/`auction_spending` already carry both the static tournament data *and* the live auction state together, so one snapshot of the current `players`/`teams` lists captures everything Milestone 9 asked for (tournament snapshot, team state, and SOLD/UNSOLD state) with no separate representation. `Auction.to_dict()` already carried `queue`, `current_queue_position`, `history`, `status`, `random_seed`, and `round_number` from earlier milestones — nothing there needed to change either.
- **Snapshot immutability**: loading a save only ever reconstructs `Player`/`Team` objects from the save file's own embedded JSON — it never re-reads `data/players.json`/`data/teams.json`. A later canonical data change (a rating fix, a new photo, an organizer roster edit) can therefore never retroactively alter an already-saved session; only a brand-new session (`AuctionSession.start()`, which does call `load_players()`/`load_teams()`) picks up the newest canonical data. Verified by dedicated tests that mutate a temp canonical-data fixture between saving an old mock and creating a new one.
- **Tournament-rule constants (minimum legal price, GK assumptions, squad size) are not duplicated into the snapshot** — they're fixed business logic implemented in code (`models/team.py`'s `MINIMUM_LEGAL_PRICE`, `services/auction_service.py`'s GK/eligibility checks), not per-session configuration, so a restored session is governed by whatever rules the *running app version* implements, exactly like `max_squad_size`/`starting_budget` already were via each `Team`'s own fields.
- **Schema version**: `persistence_service.SCHEMA_VERSION = 1`, required in every save file. Loading rejects (via `PersistenceError`, never a raw exception) a missing, non-integer, newer, or older schema version with a clear message — no migration logic exists yet, as instructed ("do not invent migrations").
- **Atomic writes** (`atomic_write_json`): serialize to a `<name>.json.tmp` file in the same directory, flush, `os.fsync`, close the handle, then `os.replace` it over the target — atomic on both POSIX and Windows for a same-directory rename, and the handle is fully closed (required on Windows) before the replace happens. If serialization itself fails partway (a bad value, a simulated fault), the exception propagates *before* `os.replace` ever runs, so a previously valid save is always left completely untouched — verified directly (write valid data, then force a failing write, then assert the original bytes are unchanged) and via `save_session` wrapping any `OSError`/`TypeError`/`ValueError` into `PersistenceError` without advancing `session.updated_at`.
- **Autosave triggers**: `AuctionSession` gained an injectable `autosave: Callable[[AuctionSession], None] | None` hook (wired once, by `ui/main_window.py`, to `persistence_service.autosave_hook`; left `None` — meaning no disk I/O at all — in every existing test fixture that builds a session directly, so none of the ~450 pre-Milestone-9 tests touch disk). `_trigger_autosave()` runs after `start()` (the "persist a new session immediately" requirement), and after every *successful* `sell_current_player()`/`mark_current_player_unsold()` — which automatically covers SOLD, UNSOLD, round transitions, BLOCKED, and COMPLETE, since all of those are just different outcomes already reflected in `session.auction`/`session.teams` by the time the call returns. A rejected transaction raises before `last_result` is even updated, so it never reaches the autosave call — both in-memory and persisted state are provably unchanged for a failed sale (`test_rejected_transaction_does_not_alter_persisted_save`).
- **Autosave failure never rolls back a transaction**: `_trigger_autosave()` deliberately catches any exception the hook raises (a broad `except Exception` at this one specific boundary, explained in its own docstring, is intentional here — the alternative would be letting a disk failure crash or silently corrupt an auction that has already legitimately succeeded in memory) and records it on `session.last_save_error` instead of propagating. The Live Auction screen surfaces this immediately and prominently as a red "SAVE FAILED" indicator — the price is never silently treated as saved when it isn't.
- **Restore path**: `persistence_service.load_session(path)` is the *one* way to go from a save file back to a ready `AuctionSession` — it validates the file's shape, schema version, and session mode; reconstructs `Player`/`Team`/`Auction` via their own `from_dict()` (reusing each model's own `__post_init__` invariant checks — valid positions, non-negative budgets, no duplicate roster/queue ids — instead of re-implementing any of that); then cross-validates ids that only make sense *between* those objects (every roster/queue/history player id resolves within the snapshot, every captain resolves to a player actually marked `is_captain`, every SOLD entry's team name resolves) — deliberately validated only against the snapshot's own embedded data, never against today's `data/players.json`. Any failure raises a single `PersistenceError` type with a readable message; a raw `OSError`/`JSONDecodeError`/`KeyError` never escapes. Because `AuctionSession`/`MainWindow` identity must stay stable across screens, `AuctionSession.adopt(other)` copies a freshly-restored session's data into the *existing* shared session object in place (deliberately not copying `autosave`, so the app's existing wiring survives a resume) rather than the UI trying to swap object references across every screen.
- **Live Auction session launcher** (`ui/screens/live_auction_screen.py`'s "not started" view, replacing the old plain "START AUCTION" button — `_on_start_auction_clicked` is kept as a stable internal alias for "start a new MOCK session," since dozens of pre-existing tests call it directly): "NEW MOCK AUCTION" / "NEW LIVE AUCTION" buttons, plus a scrollable "Saved Sessions" panel with a LIVE section (the current `live_active.json`'s metadata and a Resume button, or "No active LIVE auction yet.") and a MOCKS section (every saved mock, newest first, each with its own Resume/Delete). A corrupt save renders as a clearly labeled "SAVE CORRUPT / UNREADABLE" card with only a Delete action — the listing never crashes because one file is bad (`persistence_service.summarize_save_file` never raises). Clicking "NEW LIVE AUCTION" while a LIVE save already exists opens a confirmation dialog explaining the existing session will be *archived, not deleted*, before `persistence_service.archive_live_session()` runs and a fresh LIVE session starts — starting a new MOCK never touches LIVE at all, so no confirmation is needed there. Deleting a saved session (mock or a corrupt file of either mode) goes through the same confirmation-dialog pattern SOLD/UNSOLD already used.
- **Session-mode + save-status indicators**: once a session is active, a compact badge row (added to the in-progress/complete/blocked views alongside the existing progress row) shows "MOCK SESSION" (muted gray) or "LIVE SESSION" (solid red — deliberately unmistakable) plus "Autosaved `<time>`" / "Not yet saved" / "SAVE FAILED" (red) — small enough not to compete with the player card on a projector.
- **Blocked/complete/re-auction all resume correctly**: a restored session is a real, fully-reconstructed `AuctionSession` — the same object type `sell_current_player`/`mark_current_player_unsold`/the GK rule/the budget reserve rule/blocked-state detection already operate on — so nothing about the Live Auction or Teams screens needed to know a session was resumed rather than freshly started. Verified directly: a resumed session can process another SOLD, can process UNSOLD into a fresh re-auction round, still rejects a second GK to a team that already has one, and still enforces the budget reserve rule's `maximum_legal_bid` after resuming.
- **Tests**: `tests/test_persistence_service.py` (50 tests) covers save/load content fidelity (schema version, mode, players/teams/queue/position/round/history/budgets/rosters/spending/SOLD-ids/BLOCKED/COMPLETE), restored-session gameplay (another SOLD, UNSOLD/re-auction, GK rule, budget reserve rule), autosave triggers and atomicity (including a rejected transaction leaving the save untouched, and a simulated mid-write failure leaving the previous file byte-identical), corrupt/unsupported-schema handling (invalid JSON, wrong shape, bad model records, unresolvable cross-references, wrong schema version — each via `PersistenceError`, never a crash), multi-session management (separate mocks, mock-cannot-overwrite-live, independent deletion, live-save detection, a completed session still loading), canonical-data snapshot immutability, and a full end-to-end "sell some, mark one unsold, save, discard in-memory objects, reload, verify exact match, continue, sell again, reload again, verify again" recovery scenario. `tests/test_live_auction_screen.py` gained 12 more tests wiring the same behavior through the actual screen (launcher buttons, resume, delete-with-confirmation, the new-LIVE archive confirmation, mode/save-status badges, and the "autosave failure never blocks a successful transaction" policy).
- Full regression: `python -m pytest -v` — **463 passed, 2 skipped** (the same 2 pre-existing, unrelated Tk-environment skips from every prior milestone), 0 failed. No existing test began touching real disk: every fixture that builds an `AuctionSession` directly leaves `autosave` at its default `None`, and only `MainWindow` (real app usage) or a test's own explicit, `tmp_path`-isolated fixture wires the real hook.

## Player Cards + Last-Season FPL — September 2026

A focused UI/data feature pass (not Milestone 10 — no auction business logic, persistence architecture, ratings, positions, captain assignments, team data, budget rules, or GK rules were touched). Adds a visual player gallery and one new nullable statistic.

- **New sidebar screen, Player Cards** (`ui/screens/player_cards_screen.py`), registered in `ui/screens/SCREEN_BUILDERS` and `ui/main_window.py`'s `NAV_ITEMS` between Live Auction and Players & Setup — sidebar order is now Live Auction, Player Cards, Players & Setup, Teams, Auction History, Reports, Settings. Deliberately separate from Players & Setup: that screen stays the administrative, table-style roster view; Player Cards is the visual, sports-broadcast-style showcase — one reusable compact-card component rendered once per player rather than 32 hand-built widgets, matching how `ui/screens/players_setup_screen.py`'s table rows and `ui/screens/teams_screen.py`'s team cards are already built from one row/card function.
- **Session-aware like `teams_screen.py`**: before a session is active, loads canonical `data/players.json`; once a session is active, shows that session's live in-memory `Player` objects instead, so the detail view's current-status line can reflect a real SOLD/UNSOLD outcome. Entirely read-only either way — nothing here ever calls `process_sale`/`process_unsold` or mutates a `Player`/`Team`/`Auction` object; filtering, searching, and sorting only ever read.
- **`last_season_fpl_points: int | None = None`** added to `models/player.py`'s `Player` dataclass — the organizer's previous-season FPL point total, sourced from the organizer's previous-season result sheet. `None` means "no usable last-season result" and is rendered as `N/A`; it is never `0`, which is a real, distinct score (a player who genuinely scored 0 last season would store `0`, not `None`). A purely additive dataclass field: `Player.to_dict()` (still plain `asdict(self)`) and `Player.from_dict()` (still `cls(**data)`) needed **zero code changes** — a dict missing the key (an old canonical record, or an old Milestone 9 save file predating this field) simply falls back to the dataclass default of `None` when unpacked as `**data`, and a dict that does include it round-trips normally. No schema-version bump was needed in `services/persistence_service.py` either, since its schema validation only checks top-level save-file keys, not per-player fields — exactly the "backwards-compatible additive field, no migration" outcome the ticket asked for.
- **All 32 canonical players now carry this field** in `data/players.json` (22 explicit organizer-supplied values, 10 explicit `null`s for players without a usable prior-season result — see "AUTHORITATIVE LAST-SEASON POINTS" below); no other field on any record changed.
- **Filters**: one segmented control — `ALL`/`GK`/`DEF`/`MID`/`ATT`/`CAPTAINS` — a single dimension (not position-and-status independently) where `CAPTAINS` shows exactly the 4 captains regardless of position and every other value filters by position regardless of captain status. Implemented by translating the selected value into `services/player_service.filter_players()`'s existing `position`/`status` parameters (`CAPTAINS` → `status="CAPTAIN"`, everything else → `position=<value>` or neither for `ALL`) rather than writing a second, competing filtering implementation.
- **Search**: full/short name, case-insensitive, also via the existing `filter_players()` — composes with the position/captain filter exactly like Players & Setup's search already does.
- **Sort**: `OVR High→Low/Low→High`, `FPL High→Low/Low→High`, `Name A→Z`, implemented as a small local pure function (`_sort_players`) since no shared sort utility existed yet. FPL sorts use a `(is_missing, value)` tuple key so a `None` FPL value always sorts after every real value in **both** directions — `None` is never coerced to `0` for ordering purposes.
- **Compact card**: real photo (via the existing `ui/widgets.load_cover_fit_image` — no second photo-loading implementation), position badge (via the existing `ui/widgets.position_badge`, so GK/DEF/MID/ATT colors stay identical to every other screen), OVR, full name, short name, "FPL LAST SEASON: `<value>`/N/A", and a CAPTAIN badge for the 4 captains. One click-target: `<Button-1>` is bound recursively across the whole card (photo, labels, padding) so clicking anywhere opens the detail view, not just one sub-label.
- **Responsive grid**: column count is computed from the screen frame's own current width (`self.winfo_width()` — already the content-area child, sidebar excluded) divided by a fixed card width+gap, clamped to 2–6 columns, recomputed on every filter/search/sort refresh and via a debounced `<Configure>` binding (only triggers a rebuild when the *column count itself* would change, not on every pixel of a drag). The `<Configure>` binding also fixes a real timing bug found while building this: at construction time (before `MainWindow` has gridded the screen into its content area, or in a withdrawn test root) `winfo_width()` isn't meaningful yet, so the very first render used a safe fallback of 4 columns; the binding corrects it automatically once the geometry manager actually places/resizes the frame, without ever needing the screen to know about `MainWindow`'s sidebar width directly.
- **Detail (expanded) view is an in-page panel, not a dialog.** A prototype using a `CTkToplevel` (mirroring Live Auction's SOLD/UNSOLD confirmation-dialog pattern) hit a real, reproducible CTk/DPI-scaling bug on a scaled display: `CTkToplevel.geometry()` re-applies window-scaling on top of `winfo_reqwidth()`/`winfo_reqheight()`, which are *already* physically-scaled values — sizing the dialog from its own measured request size caused it to render markedly taller than requested (865 logical → 1081 actual in testing) and, separately, its size kept drifting across redraw cycles independently of that. Both are specific to a `CTkToplevel` carrying a large `CTkImage`; Live Auction's existing text-only confirmation dialogs are small enough that the same latent issue is harmless there. Rather than patch around an unstable moving target (or touch the working, already-tested Live Auction dialog code), the expanded view is implemented as this same screen swapping its gallery grid for a detail layout (`_open_detail`/`_close_detail`, mirroring the state-machine `_render()` dispatch pattern already used by `live_auction_screen.py`) — a plain child frame with no separate window geometry to fight, wrapped in a `CTkScrollableFrame` so a tall detail panel (a larger photo plus every field) scrolls cleanly instead of clipping on a short window.
- **Expanded view fields**: larger real photo, full name, short name, position badge, OVR, "LAST SEASON FPL", a CAPTAIN badge when relevant, and one current-status line — `CAPTAIN — <team>` for captains, or (when a session is active) `SOLD — <team> — <price>M` / `UNSOLD — awaiting re-auction` / `UNASSIGNED` derived directly from the `Player`'s own `auction_status`/`sold_to`/`sold_price` fields (already updated in place by `services/auction_service.py` since Milestone 7 — no new cross-referencing logic needed), or `PRE-AUCTION` when no session is active. Deliberately does **not** show age, height, preferred foot, nationality, or any biography — no such organizer data exists, and none was invented.
- Verified end-to-end: `python -m pytest -v` — **541 passed, 2 skipped** (the same 2 pre-existing, unrelated Tk-environment skips from every prior milestone), 0 failed — and a scripted `python main.py` walkthrough confirming the exact examples from the ticket (Rizvi Ibrahim DEF 90 OVR FPL 111; Munem MID 89 OVR FPL N/A; Samin Haque ATT 87 OVR FPL 80 with a CAPTAIN badge; Masrur Rahman GK 88 OVR FPL N/A) render correctly with real photos, correct badges, and no clipping, plus the CAPTAINS filter returning exactly the 4 captains and the detail panel's Back button returning cleanly to the gallery.
- New `tests/test_player_fpl_points.py` (46 tests: every explicit organizer value, every no-data player confirmed `None` not `0`, legacy-record/round-trip backward compatibility, negative-value rejection) and `tests/test_player_cards_screen.py` (29 tests: loading, all four position filters, the captain filter, search, all five sort orders including the None-sorts-last guarantee in both directions, real-photo loading, missing/corrupt-photo fallback, the detail view's content and back-navigation, and read-only guarantees). `tests/test_persistence_service.py` gained 2 more tests confirming an old save file missing the new field loads safely (`None`) and a new save preserves an explicit FPL value.

## Auction History — September 2026

A focused UI/data feature pass (not Milestone 10 — no auction business logic, persistence architecture, player data, ratings, FPL points, photos, team data, GK rules, budget reserve rules, re-auction logic, Player Cards, Settings, or Reports were touched). Turns the eighth sidebar route, **Auction History**, into a real, read-only, chronological transaction-timeline screen for the currently loaded session — deliberately the event log, not the analytics/summary dashboard (that remains Reports' future job).

- **Single data source, no second history store**: `ui/screens/auction_history_screen.py` reads directly from `session.auction.history` (the same `list[AuctionHistoryEntry]` `services/auction_service.py`'s `process_sale`/`process_unsold` already append to) via a new `services/auction_history_service.py` — pure filter/sort/summary functions, no file I/O, nothing mutated. There is no duplicate transaction log anywhere.
- **Attempt-based, not player-based**: history is never deduplicated by `player_id`. A player UNSOLD in round 1, UNSOLD again in round 2, then SOLD in round 3 appears as three independent rows, in order — exactly what `AuctionHistoryEntry` already recorded before this ticket; this pass only adds a screen that reads it faithfully.
- **New additive field, `AuctionHistoryEntry.round_number: int | None = None`** (`models/auction.py`), populated by `services/auction_service.py` from `auction.round_number` at the moment each entry is created (before `_finish_transaction` advances the queue or starts a new round), so it always reflects the round the attempt actually happened in. Purely additive: `Auction.from_dict()`'s existing `AuctionHistoryEntry(**entry)` reconstruction already falls back to the dataclass default for a dict missing the key, so an old Milestone 9 save file predating this field loads safely with `round_number is None` on its legacy entries — no schema-version bump, no migration. The screen renders a legacy entry's round as "—" rather than guessing a value.
- **Screen behavior**: no active session shows a plain "NO ACTIVE AUCTION SESSION" empty state (never auto-starts one); an active session with zero transactions shows "NO TRANSACTIONS YET"; COMPLETE and BLOCKED sessions remain fully readable and filterable, exactly like IN_PROGRESS. A compact context row shows the MOCK/LIVE badge, current round, `SOLD n / 28`, total attempt count, and the session name.
- **Summary cards** (session-wide, not affected by the current filter): Total Attempts, Sold, Unsold Attempts, Total Spent. "Unsold Attempts" counts every UNSOLD *attempt*, not distinct still-unsold players (a player UNSOLD twice contributes 2); "Total Spent" sums only SOLD prices.
- **Filters/search/sort** (`services/auction_history_service.filter_history`/`sort_history`): result (ALL/SOLD/UNSOLD), round (ALL ROUNDS or a specific round, options built dynamically from what's actually in history), and team (ALL TEAMS or one of the session's four teams — filtering to a specific team naturally excludes every UNSOLD row, since an UNSOLD entry's `team` is always `None`, matching the rule that UNSOLD attempts are never attributed to a team). Search matches full name (always available) or short name (resolved from the session's live `Player` snapshot, since a history entry itself only stores the name at the time of the attempt), case-insensitive. Sort defaults to Oldest → Newest (by `auction_sequence` — a timeline, never re-ordered by price or player name); Newest → Oldest is the only other option.
- **Rows**: sequence number, a small photo thumbnail (reusing `ui/widgets.load_cover_fit_image` — no second photo loader), player name, position badge, a SOLD (green) / UNSOLD (red) result badge, a neutral round badge, buying team or "—", and sale price (gold, bold) or "—". No timestamp column — history entries don't store one, and none was invented.
- **Entirely read-only**: no undo, edit, delete, or reassignment control exists anywhere on this screen; a dedicated test (`test_screen_does_not_mutate_auction_session`) exercises every filter/search/sort control and then asserts the session's history, teams, and players are byte-for-byte unchanged.
- **Persistence**: no new persistence code was needed — history already persists as part of the existing `AuctionSession`/`Auction` snapshot (Milestone 9), and `round_number`'s backward-compatible default means an old save loads exactly as safely as before. Verified end-to-end: save a session with transactions, discard the in-memory object entirely, reload via `services/persistence_service.load_session`, and confirm the screen renders byte-identical rows.
- New `tests/test_auction_history_service.py` (24 tests: summary math, round-number listing, every filter individually and combined, search by full/short name, both sort orders, multi-attempt-per-player independence) and `tests/test_auction_history_screen.py` (31 tests covering the full ticket checklist: no-session/empty-history states, SOLD/UNSOLD row content, round 2/3 re-auction rows, every filter/search/sort control, summary-card correctness, save/resume identical history, session switching, COMPLETE/BLOCKED readability, and the read-only guarantee). `tests/test_auction.py` and `tests/test_auction_service.py` gained tests for the new `round_number` field (default/explicit/validation, backward-compatible legacy-dict loading, and correct stamping across a forced round-1 → round-2 re-auction). `tests/test_persistence_service.py` gained 2 tests mirroring the FPL field's precedent: an old save missing `round_number` loads safely, and a new save preserves it. `tests/test_live_auction_screen.py`'s placeholder-lock test was renamed/updated since Auction History is no longer a placeholder (further renamed again by the Reports ticket below — see that section).
- Verified end-to-end: `python -m pytest -v` — **610 passed, 2 skipped** (the same 2 pre-existing, unrelated Tk-environment skips from every prior milestone), 0 failed — and a scripted `python main.py` walkthrough at 1024×640, 1366×768, and 1920×1080 confirming correct SOLD/UNSOLD rows, round labels, team/price display, and summary totals for a real mock session with re-auction activity. That walkthrough caught one real (fixed) responsiveness bug: the original single-row filter bar (search + 4 controls + count label) overflowed the app's 1024px minimum window width with no wrapping, clipping the sort dropdown — fixed by splitting filters across two rows (search/result/count on one line, round/team/sort on the next), verified clean at all three sizes afterward.

## Reports — September 2026

A focused UI/data feature pass (not Milestone 10 — no auction business logic, persistence architecture, player ratings, positions, FPL values, team data, GK rules, budget reserve rules, re-auction logic, Auction History, Player Cards, or Settings were touched). Turns the ninth (and final Milestone-4-era) sidebar route, **Reports**, into a real, read-only auction summary/analytics dashboard for the current session — deliberately the aggregated-statistics/team-comparison view, never a row-per-transaction timeline (that remains Auction History's job).

- **New dedicated analytics service, `services/report_service.py`** — pure, read-only functions/dataclasses over a session's own `players`/`teams`/`auction.history` snapshot; nothing here mutates any of them, and nothing merges data from another session. `calculate_price_statistics` (SOLD-only average/median/highest/lowest, with tie-lists for highest/lowest — UNSOLD entries never contribute a price, not even as an implicit 0), `calculate_reauction_statistics` (UNSOLD attempts, unique UNSOLD players, eventually-sold-after-UNSOLD, max attempts for one player, rounds reached), `calculate_fpl_statistics` and `calculate_ovr_statistics` (both SOLD-only, `None` FPL always excluded from averages rather than coerced to 0), `build_team_report` (one team's full summary + roster), `build_auction_summary` (session-wide KPIs), and `build_full_report(session)` (the single call the screen makes, returning one `AuctionReport` dataclass bundling everything above).
- **Team purchase data is read from each `Player`'s own `sold_price`/`auction_status` fields, never re-derived from `auction.history` per team.** Since SOLD is the sole permanent outcome (a player can only ever be SOLD once), this makes "don't double-count a player re-auctioned from an earlier UNSOLD round" structurally impossible to get wrong, rather than something requiring careful history-scanning logic — `Team.roster` already contains exactly the players that team currently owns, each contributing exactly one purchase price. Global/session-wide price statistics use `auction.history` directly instead (a natural flat list of every attempt); both approaches necessarily agree, since `process_sale` sets a player's `sold_price` and that same transaction's history entry's `sold_price` atomically from one value.
- **Captain handling, applied consistently everywhere**: captains are excluded from auction spending, average/highest/lowest purchase price, and purchase-only OVR/FPL averages (a captain never appears in `auction.history` at all, so history-based statistics exclude them for free; `build_team_report` additionally filters roster-based purchase statistics on `not player.is_captain`). Captains **are** included in final squad size and in the separate "squad" OVR/FPL averages (`average_ovr_squad`/`average_fpl_squad`), which are explicitly named and displayed as squad-based, never labeled as purchase averages. A captain's roster entry always renders as `CAPTAIN` (`RosterEntry.role`, `price=None`) — never as a "0M purchase."
- **KPI row**: Total Sold (`n / 28`), Total Spent, Avg Sale, Highest Sale, Rounds — plus a secondary "Additional Metrics" panel (Sold %, Remaining Unsold, Total Attempts, Median Sale, Lowest Sale, Teams Full `n / 4`, Combined Remaining Budget) kept visually smaller/denser than the KPI row so the first screen isn't overloaded with every metric at equal visual weight.
- **Highlights**: Most Expensive, Cheapest Sold, and Highest-Rated Purchase, each showing player/team/position/OVR/price. A tie (two or more SOLD entries sharing the extreme price or rating) is never arbitrarily hidden — the card shows "N players tied" plus a listing of every tied player and team.
- **FPL analytics** (purely descriptive, per the ticket's explicit rule — FPL never affects any auction rule): highest-FPL SOLD player and value, average FPL of sold players with a **known** FPL only, and an FPL N/A count so a `None` value is visible as excluded rather than silently dropped.
- **OVR analytics**: average OVR of SOLD players (auction purchases only — captains structurally excluded, see above) and the highest-OVR SOLD player/value, with the same tie-list handling as price highlights.
- **Re-auction summary panel**: UNSOLD attempts, unique UNSOLD players, eventually-sold-after-UNSOLD, and rounds reached — reuses `services/report_service.calculate_reauction_statistics`, not a second calculation. Shows a clean "No re-auction activity yet." message (`ReauctionStatistics.has_activity`) instead of a row of zeros when nothing has gone to a second round.
- **Team comparison bars** (Team Spending, Remaining Budget, Squad Size, Average Purchase Price): plain nested `CTkFrame`s (`.place(relwidth=fraction)` for the colored fill) rather than a canvas or an external charting library, per the ticket's explicit preference — each group's bars are normalized against that group's own max value across the four teams.
- **Per-team panels** (2×2 grid, `TEAMS_PER_ROW = 2`, same static-grid pattern `ui/screens/teams_screen.py` already uses successfully at every supported window size): captain, squad `n/8`, total spent, remaining budget, GK status (YES/NO via the existing `services.auction_service.team_has_goalkeeper` — not recomputed locally), purchases count, average/highest/lowest purchase price (all `N/A` rather than `0`/an error for a team with zero purchases so far), and `team.maximum_legal_bid` (the existing budget-reserve-rule property, reused directly rather than re-implemented) shown as "Max Next Bid" — useful mid-auction without turning Reports into a second bidding screen. Below that, the full current roster (name, position badge, OVR, price or `CAPTAIN`).
- **Works for IN_PROGRESS, COMPLETE, and BLOCKED alike** — nothing here assumes all 28 players are sold; a BLOCKED session additionally shows a red `BLOCKED` status badge in the context row (alongside the existing MOCK/LIVE badge, round label, and `SOLD n/28`) but every metric/panel remains fully readable and correct for whatever has actually happened so far.
- **Entirely read-only**: no edit/undo/delete/reassign/price-change control exists anywhere on this screen; a dedicated test (`test_screen_does_not_mutate_auction_session`) exercises the screen twice and asserts the session's history, teams, and players are byte-for-byte unchanged.
- **No active session** shows a plain "NO ACTIVE AUCTION SESSION" empty state (never auto-starts one), matching Auction History's established pattern exactly.
- **Persistence**: no new persistence code — Reports derives everything fresh from the restored `AuctionSession` snapshot every time the screen is built, so a resumed session produces identical report data automatically (verified end-to-end: save a session with transactions, discard the in-memory object entirely, reload via `services/persistence_service.load_session`, confirm every KPI/team figure matches, then perform another sale and confirm the very next report build reflects it immediately).
- **Export explicitly deferred**, as instructed — no PDF/CSV output exists yet. `report_service.py`'s plain, frozen dataclasses (`AuctionSummary`, `PriceStatistics`, `TeamReport`, etc.) were chosen specifically so a future export feature could serialize them directly without restructuring any calculation.
- **A real 1024×640 layout bug was found and fixed during manual verification**: each team panel's 8 mini-stats were originally laid out 4-per-row, which clipped longer labels/values ("Max Next Bid", "Avg Purchase", a longer player name like "Aafeef Kabir") at the app's minimum supported window width, since a team panel is itself only about half the window wide in the 2×2 grid. Fixed by switching to a 2-column, 4-row layout for the same 8 stats — verified clean at 1024×640, 1366×768, and (screen-resolution-permitting) 1920×1080 afterward.
- New `tests/test_report_service.py` (48 tests: price statistics including ties and the minimum-legal-price edge case, median via both odd/even counts, re-auction statistics, FPL statistics with `None` correctly excluded from every average, OVR statistics and ties, per-team report correctness including the full captain-handling rule set and GK status, and full-session integration tests covering IN_PROGRESS/COMPLETE/BLOCKED, resumed-session identical data, and the no-mutation guarantee) and `tests/test_reports_screen.py` (12 GUI-level tests: empty states, real-transaction rendering, IN_PROGRESS/COMPLETE/BLOCKED readability, session switching, save/resume identical report, metrics updating immediately after a new post-resume sale, and the read-only guarantee). `tests/test_live_auction_screen.py`'s placeholder-lock test was renamed again (`test_settings_remains_a_placeholder`) since Reports is no longer a placeholder — Settings was, at the time of this ticket, the only sidebar route left showing Milestone 4 placeholder text (that test was renamed once more — `test_no_sidebar_screens_remain_placeholders` — once Settings itself became a real screen; see "Settings — September 2026").
- Verified end-to-end: `python -m pytest -v` — **670 passed, 2 skipped** (the same 2 pre-existing, unrelated Tk-environment skips from every prior milestone), 0 failed — and a scripted `python main.py` walkthrough with a realistic mock session (multiple SOLD at varied prices across all four teams, an UNSOLD, and a player forced through a round-1 UNSOLD → round-2 SOLD re-auction, reaching full COMPLETE 28/28) confirming correct KPI totals, additional metrics, tied/untied highlights, FPL/OVR analytics, re-auction summary, comparison bars, and all four team panels/rosters, cross-checked manually against the equivalent Auction History figures for the same session.

## Live Auction Premium Player Card — September 2026

A focused UI enhancement (not Milestone 10 — no auction business logic, persistence, player ratings/FPL values/positions, team data, GK rules, budget reserve rules, re-auction logic, Reports, Auction History, or Settings were touched). Reuses the premium Calgary-Vipers card visual language already built for the Player Cards gallery inside the Live Auction current-player reveal, so the two screens now feel like one consistent design system instead of two unrelated card styles.

- **Extracted the shared card-art system into `ui/player_card_art.py`**, a new UI-layer module (pure presentation, like `ui/theme.py`/`ui/widgets.py` — no business logic) holding everything `ui/screens/player_cards_screen.py`'s gallery card used to define privately: `card_variant`, `fpl_display`, `VARIANT_COLORS`, `build_card_background` (the composited Vipers green/red diagonal-sweep PIL background, cached per `(width, height, corner_radius, variant, is_captain)` — a plain tuple key that now spans every screen using this module, not just one), `frame_photo`/`photo_placeholder_panel` (rounded, colored-bordered photo framing), and `build_player_card_image` (the full per-player composite: background + real framed photo or placeholder panel). Every function takes its size/geometry as parameters rather than assuming one fixed layout, which is what let Live Auction adopt the same visual system at a different size without forking the drawing code.
- **`ui/screens/player_cards_screen.py` was refactored, not rewritten**, to call the shared module — its own `_VARIANT_COLORS`/`_card_variant`/`_hex_to_rgb`/`_frame_photo`/`_build_card_image`/`_fpl_display` names all still exist (thin wrappers or direct re-exports, fixed to this screen's own gallery-tile constants) so nothing importing them — including existing tests — needed to change. Verified byte-for-byte behavior-preserving: the full `tests/test_player_cards_screen.py` suite (33 tests) passes unchanged, and a before/after screenshot of the gallery at 1366×768 is pixel-identical.
- **New Live Auction card variant** (`ui/screens/live_auction_screen.py`'s `_build_player_card`): real photo, full name, position badge, OVR, and **last-season FPL** — deliberately nothing else (no short name, no captain badge, no biography/age/height/nationality/FIFA attributes/auction history/detailed stats), per the ticket's "essential auction information only" rule. Bidding controls (team buttons, price entry, SOLD/UNSOLD, confirmation dialogs, budget/max-legal-bid display, save status, round info, MOCK/LIVE badge, progress) are unchanged rows below the card — this pass only replaced the current-player visual area.
- **Two size variants, not one fixed layout** (`_STANDARD_CARD_GEOMETRY` at 300×490 for the app's default 1366×768/1920×1080, `_COMPACT_CARD_GEOMETRY` at 172×266 for the 1024×640 minimum window), chosen by `_choose_card_geometry(window_width)` — a pure function (no widget access) called with `self.winfo_toplevel().winfo_width()` at render time, so it's directly unit-testable without a real window at a specific size. Neither variant reuses the gallery's exact 240×420 tile dimensions, per the ticket's explicit allowance for a differently-sized variant.
- **FPL display rule**: `FPL {fpl_display(player.last_season_fpl_points)}` — a known value renders as `FPL 70`; `None` (no usable last-season result) renders as `FPL N/A`, never `FPL 0`. A player whose FPL is genuinely `0` (a real, distinct score) would correctly render `FPL 0` — verified directly with a synthetic override, since no canonical player currently has a stored `0`.
- **Real photos only, same shared loader**: `build_player_card_image` calls the existing `ui/widgets.cover_fit_pil_image` internally (no second image-loading/cropping implementation) and falls back to the same initials-placeholder pattern already used elsewhere when a photo is missing/corrupt — verified via the pre-existing missing/corrupt-photo tests, which still pass unchanged.
- **A real overlap bug was found and fixed during manual verification**: the first geometry draft budgeted only enough vertical space between the name and the badge row for a single line of text. A long name ("Shahriar Anwar Khan", "Aiman Nawar Chowdhury") wraps to two lines at the card's larger font size (unlike the gallery's much smaller font, where this never surfaced), and the second line collided with the position/OVR badges below it. Fixed by widening that gap in both geometry variants to comfortably fit a two-line wrap.
- **A second real bug was found and fixed at 1024×640**: the first compact-card size (172×266 was reached after two rounds of shrinking from an initial 230×390) — the initial compact card was tall enough that, combined with the progress row, team-status row, price controls, and SOLD/UNSOLD buttons, the action buttons were pushed below the visible window with no scrolling on this screen. Fixed by shrinking the compact variant specifically (smaller photo, smaller fonts, tighter spacing) until every row fit cleanly — verified via screenshot that SOLD/UNSOLD are fully visible and unclipped at exactly 1024×640.
- **Caching and GC-safety, reusing established patterns**: `build_card_background`'s cache means repeatedly advancing through 28 reveals (plus SOLD/UNSOLD/re-auction) regenerates each distinct decorative background at most once per (size, variant, captain-flag) combination — at most 4 per geometry variant — never once per reveal. `LiveAuctionScreen` keeps exactly one reference each for `_current_card_art_image` (the whole composited card, always present) and `_current_photo_image` (non-`None` only when a real photo was used — preserved for the 4 pre-existing photo tests that assert on it directly), both reassigned outright on every render rather than appended to a list, so nothing accumulates across repeated player advancement — the previous player's images become eligible for garbage collection the instant these are overwritten.
- New `tests/test_player_card_art.py` (16 tests: hex conversion, variant determinism, FPL display including the real-zero-vs-None distinction, background caching/sizing/variant/captain differentiation, photo framing/placeholder sizing, and the full composite with a missing path, a missing file, and a real canonical photo). `tests/test_live_auction_screen.py` gained 21 tests covering the full ticket checklist (shared-helper usage, real photo loading, position/OVR/FPL display including all four named examples from the ticket, the None-never-renders-as-0 guarantee alongside a real-zero counter-example, FPL/photo/name/OVR/position all updating together on advance, stale image references replaced safely, the missing-photo fallback still working, the geometry-selection pure function, and that bidding controls/team selection/SOLD/UNSOLD/max-legal-bid display all still function with the new card).
- Verified end-to-end: `python -m pytest -v` — **707 passed, 2 skipped** (the same 2 pre-existing, unrelated Tk-environment skips from every prior milestone), 0 failed — and a scripted `python main.py` walkthrough forcing the current player to each of the ticket's named examples (Shahriar Anwar Khan MID 84 OVR FPL 70, Rizvi Ibrahim DEF 90 OVR FPL 111, Munem MID 89 OVR FPL N/A, Masrur Rahman GK 88 OVR FPL N/A) at 1366×768 and 1024×640, confirming the Vipers card background, correct undistorted photo, readable name/position/OVR/FPL, and no clipping of the bidding controls at either size.

## Settings — September 2026

A focused UI feature pass (not Milestone 10 — no auction business logic, persistence architecture, player data, ratings, FPL values, team data, GK rules, budget reserve rules, re-auction logic, Reports, Auction History, Player Cards, or phone-bidding implementation were touched). Turns the tenth and final Milestone-4-era sidebar route, **Settings**, into a real organizer configuration/diagnostics screen — deliberately conservative about what it lets the organizer actually change.

- **Core tournament rules remain entirely locked**, as required: the "Tournament Rules" section (32 players, 4 captains, 28 auction players, 4 teams, 100M starting budget, 8-player squad, exactly 1 GK/team, 1M minimum price, the budget reserve formula, captain assignments, SOLD/UNSOLD/re-auction behavior) is plain read-only text — a dedicated test (`test_core_rules_are_not_editable`) walks that section's entire widget tree and asserts it contains no `CTkEntry`, `CTkSwitch`, or `CTkButton`. Every value shown is sourced from existing constants/config (`services/player_service.EXPECTED_*`, `data/settings.json` via `load_settings()`, `models/team.MINIMUM_LEGAL_PRICE`) rather than re-typed as new magic numbers.
- **New dedicated preferences service, `services/preferences_service.py`**, storing organizer app preferences at `config/user_preferences.json` (gitignored, alongside `saves/`) — a file structurally separate from auction session saves, `data/players.json`, and `data/teams.json`; nothing in this module can write to any of those. `UserPreferences` (a frozen dataclass: `remember_fullscreen`, `fullscreen_enabled`, `confirm_sold`, `confirm_unsold`, the last two defaulting `True`) is read back field-by-field with its own default fallback (`UserPreferences.from_dict`), so a partially-invalid file — one bad key, one wrong-typed value — only loses that one field's setting rather than the whole file; a missing or corrupt file falls back to every field's default with no crash. Saves go through the same atomic-write-to-temp-then-`os.replace` pattern `services/persistence_service.py` already uses (duplicated at this small scale rather than imported, since preferences are a deliberately independent concern from auction persistence, not a consumer of it).
- **Fullscreen**: `ui/main_window.py`'s `MainWindow` gained `set_fullscreen(enabled)`/`toggle_fullscreen()` (wrapping the `-fullscreen` window attribute in `try/except tkinter.TclError`, since that attribute is unsupported on some platforms/backends and this is a cosmetic feature that must never crash the app) plus `<F11>` (toggle) and `<Escape>` (always exit, never toggle-into) key bindings. A remembered fullscreen preference is applied via `self.after(50, ...)` *after* the window is otherwise fully built, per the ticket's own guidance that applying it immediately risks a worse first paint than starting windowed and switching a moment later. The Settings screen's Fullscreen switch calls `self.winfo_toplevel().set_fullscreen(...)` directly (routing through the real, already-showing `MainWindow` instance rather than threading a second reference through `SCREEN_BUILDERS`'s existing `(parent, session)` calling convention) and only persists the new state to disk when "Remember Fullscreen" is also on — otherwise it behaves exactly like a one-off F11 press. Flipping "Remember Fullscreen" on captures whatever the *current* live fullscreen state is as what gets remembered from then on.
- **No fake app-wide UI scaling preset was added.** The ticket explicitly allowed a Compact/Standard/Large projector-scaling control "if practical," but this app has no existing runtime-rescaling infrastructure, and building a control that didn't actually rescale anything would be exactly the "fake functionality" the ticket separately warns against for the Captain Bidding placeholder — so it was deliberately left out rather than half-implemented. Fullscreen, the current-resolution display, and the HDMI/projector instructions were kept instead, all real and functional.
- **Auction Preferences — Confirm SOLD/Confirm UNSOLD toggles, both defaulting ON.** `ui/screens/live_auction_screen.py`'s `_on_sold_clicked`/`_on_unsold_clicked` now check `preferences_service.load_preferences().confirm_sold`/`confirm_unsold` (a fresh read each click — the screen isn't handed a live preferences reference, so this is always current with whatever Settings most recently saved) and, only when the relevant toggle is off, skip straight to `_execute_sale`/`_execute_unsold` — the exact same calls the confirmation dialog's own Confirm button already made. Every existing validation (team selection, price format, the budget reserve check, `AuctionTransactionError` handling) runs identically either way; no business-layer code was touched or duplicated.
- **Session / Save Status** reads the session already passed to every screen via the standard `SCREEN_BUILDERS` `(parent, session)` convention — current mode (MOCK/LIVE/none), session id, `auction.status`, round, `SOLD n / 28`, last autosave timestamp (via the newly-shared `ui/widgets.format_timestamp`, extracted from `live_auction_screen.py` verbatim so both screens use one implementation), save status (SAVED/SAVE FAILED/NOT YET SAVED/NO ACTIVE SESSION), and a human-readable save location via the existing `persistence_service.session_save_path`. An "OPEN SAVE FOLDER" button opens that folder with the OS's own file browser (`os.startfile` on Windows, `open`/`xdg-open` elsewhere) — checked for existence first and wrapped in `try/except OSError`, so a session that hasn't autosaved yet (no folder) or an unsupported platform is a silent no-op, never a crash. No save-file editing, deletion, or merging exists here — mock deletion already belongs to the Live Auction session launcher, unchanged.
- **Diagnostics / Build Info** reuses existing helpers rather than recomputing anything: `services.player_service.load_players()/load_teams()/validate_setup()` for player/team counts and the READY/ISSUES FOUND banner (identical wording and check list to Players & Setup's own validation), `services.persistence_service.SCHEMA_VERSION` for the save-format version, and `platform.python_version()`/`platform.system()` for the runtime info. Photo count cross-checks every player's `photo_path` against the filesystem directly (`(ROOT_DIR / path).is_file()`) and names any missing file rather than just showing a lower count. `services/config_service.APP_VERSION` (`"0.9-dev"`) is a new, plain, manually-bumped string — not tied to git tags or packaging, since neither exists yet.
- **Captain Phone Bidding was a pure future-feature placeholder at the time this Settings pass shipped** — a "NOT CONFIGURED" badge, three static rows (Server: OFF, LAN Address: —, Connected Captains: 0 / 4), and a disabled "START CAPTAIN BIDDING" button, with no server/network/authentication code. **Superseded by "Captain Phone Bidding — Phase 1 — September 2026" below**, which replaced this placeholder section with real START/STOP controls, a real LAN address, and a live connected-device count.
- **Reset App Preferences** requires a confirmation dialog (a compact, self-contained `CTkToplevel` — not the exact `LiveAuctionScreen._build_confirmation_dialog` code, to avoid touching that already-tested dialog implementation for a much lower-stakes action) and resets only `remember_fullscreen`/`fullscreen_enabled`/`confirm_sold`/`confirm_unsold` to their defaults, additionally applying `set_fullscreen(False)` to the live window for a clean, unambiguous end state. It never touches an auction session, a save file, `data/players.json`, `data/teams.json`, or photos — verified directly (`test_reset_preferences_does_not_touch_auction_saves`, mirroring the equivalent Auction History/Reports read-only guarantees).
- **A real bug was found while wiring the reset action**: the first draft re-invoked `self.__init__(self.master, self._session)` on an already-constructed `SettingsScreen` to rebuild its widget tree — unsafe, since the underlying Tk widget already exists and widgets aren't designed to be re-initialized this way. Fixed by extracting a `_render()` method (clear `winfo_children()`, reload preferences, rebuild) matching the exact pattern Live Auction/Player Cards already use for their own state-driven re-renders.
- **A real layout bug was found at 1024×640 during manual verification**: the "App Name" row in Tournament Info was originally sourced from `BrandingConfig.app_name` ("Football Auction Manager", the software product name) rather than `header_title` ("Calgary Vipers Auction 2026", the tournament-facing name the ticket's own example expects under that label) — caught by a test asserting the literal expected display text, not just that *some* text was present.
- New `services/preferences_service.py` gained `tests/test_preferences_service.py` (10 tests: safe defaults, missing/corrupt/non-object/partially-invalid file fallback, save/load round-trip, and reset not touching auction saves). New `tests/test_settings_screen.py` (32 tests covering the full ticket checklist: registration, tournament info, locked-rules read-only guarantee, fullscreen defaults/toggle/key-bindings/no-op-under-a-plain-root, remember-fullscreen persistence, confirm-SOLD/UNSOLD defaults and persistence, corrupt/missing preference fallback, reset restoring defaults and not touching saves, MOCK/LIVE/no-session session-status display, round/status/SOLD-count display, save-schema-version/player-count/team-count/photo-count/setup-validation diagnostics, the captain-bidding placeholder and its disabled button, and a no-mutation guarantee across every preference toggle plus reset). `tests/test_live_auction_screen.py` gained 5 tests confirming the confirm-off/confirm-on dialog-skip behavior and that a confirmation-off SOLD attempt still applies full budget-reserve validation; its long-standing placeholder-lock test was generalized to `test_no_sidebar_screens_remain_placeholders` (iterating every `NAV_ITEMS` entry) now that none remain.
- Verified end-to-end: `python -m pytest -v` — **753 passed, 3 skipped** (the same pre-existing, unrelated Tk-environment flakiness documented above for full-`MainWindow`-creating tests — one more such test exists now, so occasionally 3 skip instead of 2 in a full run; every one of them passes reliably in isolation), 0 failed — and a scripted `python main.py` walkthrough: opened Settings with an active MOCK session and confirmed Tournament Info, Display/Projector (including the correct physical-pixel current resolution), Auction Preferences, Session/Save Status (MOCK, session id, IN_PROGRESS, round, SOLD count), the Reset bar, Captain Phone Bidding placeholder, Diagnostics (32/32 players, 4/4 teams, 32/32 photos, schema version, TOURNAMENT SETUP: READY), and the full Tournament Rules list all render correctly and reflow cleanly at both 1366×768 and 1024×640 with no clipping.

## Captain Phone Bidding — Phase 1 — September 2026

Phase 1 goal, proved exactly: one real phone can place bids for one team over the local Wi-Fi network while the organizer places competing bids for the other teams from the laptop, and the organizer alone still finalizes SOLD/UNSOLD. Four-captain simultaneous bidding with PIN authentication, tournament standings, live tournament FPL scoring, and EXE packaging were explicitly out of scope and were not built.

- **The organizer's laptop remains the sole authority.** A phone can only view state and submit a bid amount for its selected team; nothing a phone sends can mark SOLD/UNSOLD, advance the current player, change a team's roster/budget/history, or start/end a round. Only `LiveAuctionScreen`'s own SOLD/UNSOLD buttons (organizer-operated) ever call `services/auction_service.process_sale`/`process_unsold`.
- **One authoritative live-bid state, reused rather than duplicated.** `models/auction.py`'s `Auction` dataclass already had unused `current_bid: int | None` and `leading_team_id: int | None` fields (already round-tripped by `to_dict()`/`from_dict()`); Phase 1 is the first code to actually read and write them. There is no `phone_current_bid`/`desktop_current_bid` split — the phone API, the desktop bid buttons, and the SOLD pre-fill all read/write the same two fields on the same `Auction` instance.
- **New `services/live_bid_service.py`** owns all bid validation and mutation via `place_bid(auction, players, teams, team_id_or_name, bid_amount) -> BidResult`. It re-validates, in order: auction initialized; `AuctionStatus.IN_PROGRESS` (COMPLETE → "Auction is complete. Bidding closed.", BLOCKED → "Auction is blocked. Bidding unavailable."); a current player exists; the team resolves; `services.auction_service.team_can_bid_for_player` (roster space + one-GK-per-team rule, reused, not reimplemented); bid is a non-bool `int`; bid ≥ `Team.MINIMUM_LEGAL_PRICE` (1M); bid strictly exceeds the current highest (equal is rejected, never silently raised); bid ≤ `Team.maximum_legal_bid` (the existing budget-reserve ceiling — an over-max bid is rejected outright, never clamped down to the max). A single module-level `threading.Lock` wraps the entire validate-then-mutate sequence, so two near-simultaneous bids (phone + desktop, or two phones in a future phase) can never both "win" — whichever acquires the lock second re-validates against whatever the first just committed and is rejected if it no longer qualifies.
- **`AuctionSession.place_live_bid(team_id_or_name, bid_amount)`** (`services/auction_session_service.py`) is the single shared entry point both the desktop UI and every phone API call go through — it delegates to `live_bid_service.place_bid` and triggers the existing autosave on acceptance. New `AuctionSession.current_bid`/`leading_team` read-only properties expose the live state for rendering. No second sale/bid mechanism exists anywhere else.
- **Live-bid reset on advancement, not player-scoped carryover.** `services/auction_service._finish_transaction` (called by both `process_sale` and `process_unsold`, exactly once per transaction) now unconditionally clears `auction.current_bid`/`leading_team_id` before revealing the next player — a bid can never carry from Player A to Player B. This was inlined directly into `auction_service.py` rather than added as a `live_bid_service` helper, since `live_bid_service.py` already imports from `auction_service.py` and the reverse import would be circular.
- **Local server: `services/captain_bidding_server.py`**, FastAPI + uvicorn, run in a background `threading.Thread` (daemon, `server.install_signal_handlers` overridden to a no-op since only the main thread may install signal handlers) so it never blocks the CTk `mainloop`. Binds `0.0.0.0` on port 8765; the LAN IP shown to the organizer is auto-detected (`get_lan_ip()`, a UDP socket "connected" to `8.8.8.8:80` without ever sending a packet, reading back the OS-selected outbound local address, falling back to `127.0.0.1` on any `OSError`) — the LAN IP is never hardcoded, and no OS firewall rule is ever touched programmatically. `start()` is idempotent (repeated calls do not spawn a second thread); `stop()` sets `should_exit` and joins the thread with a timeout, and is safe to call repeatedly or before ever starting. `MainWindow` owns one `CaptainBiddingServer` instance and calls `.stop()` from a `WM_DELETE_WINDOW` handler so closing the app never leaves a zombie server thread.
- **API surface** (all under the running server): `GET /health`; `GET /api/state?team_id=` — the only state a phone needs (`no_active_session`, session mode MOCK/LIVE, `auction_status`, round, `bidding_enabled`, current player's name/position/OVR/FPL/photo URL, `current_bid`, `leading_team_name`, every team's budget/squad/max-legal-bid, and the requesting phone's `selected_team`) and nothing about save-file internals; `POST /api/select-team` (Phase 1 test-mode team selection, no auth); `POST /api/bid` (calls `AuctionSession.place_live_bid` — the exact same call the desktop makes); `GET /photos/{filename}` serves player photos from `assets/players/` only, rejecting any filename containing a path separator and re-validating the resolved path is still inside that directory before returning it (no arbitrary filesystem access); `GET /` serves the phone page itself. Malformed JSON bodies are rejected automatically (HTTP 422) by the request's `pydantic` schema before any application code runs.
- **Settings screen — real Phase 1 controls**, replacing the prior placeholder described in "Settings — September 2026" above: a RUNNING/OFF status badge, live rows for Server, LAN Address (the real `http://<ip>:8765` only while running, "—" otherwise), Connected Devices (a heartbeat-based count of phones that have polled within the last 5 seconds — never a fabricated exact number when polling can't prove more), and Authentication ("TEST MODE — TEAM SELECTOR"); a START button (disabled once running) and a STOP button (disabled unless running), each visually muted when disabled rather than just Tk-disabled-but-still-bright; a COPY ADDRESS button while running.
- **Live Auction screen integration**: a compact "CAPTAIN BIDDING: RUNNING (ip:port)" / "CAPTAIN BIDDING: OFF" status line next to the existing save-status badges; a prominent CURRENT BID / LEADING TEAM row (gold/green, large text, "—"/"—" before any bid) directly above the existing team-status row; a "PLACE BID — {TEAM}" row of +1M/+2M/+5M buttons so the organizer can bid on behalf of any team from the desktop through the identical `place_live_bid` call a phone makes (this is also today's practical fallback if only one physical phone is available, and the permanent fallback for a connectivity failure or verbal bid). The screen polls `AuctionSession` every 500ms via Tk's own `after(...)` (never touching Tk widgets from the server thread — the server thread only ever touches the plain-Python `Auction`/`Team` objects guarded by `live_bid_service`'s lock) and only re-renders when a cheap state snapshot actually changed, and never while a SOLD/UNSOLD confirmation dialog is open (detected by walking `winfo_children()` for a `CTkToplevel`).
- **SOLD reuses the existing confirmation dialog and transaction service — there is no second sale path.** `_on_sold_clicked` now prefers the live bid: if `AuctionSession.leading_team`/`current_bid` are both set, those are pre-filled as the team/price passed into the existing confirmation flow; the organizer's manual team-selection + price-entry controls are otherwise unchanged and remain fully usable (server OFF, no phone connected, or a live bid never having been placed) — phone bidding is optional, never a dependency for finishing a sale.
- **A real UI-breaking bug was found and fixed while adding the new rows.** Adding the CURRENT BID/LEADING TEAM row and the desktop-bid-buttons row to the in-progress view's plain `CTkFrame` body pushed the team-status row, the manual price controls, and the SOLD/UNSOLD buttons entirely off the bottom of the window at the app's own default 1366×768 size, with no way to reach them — caught by screenshot, not by the passing test suite. Fixed by changing that body to a `CTkScrollableFrame` (verified by screenshot at both 1366×768 and 1024×640 that every control, including SOLD/UNSOLD, is reachable by scrolling).
- **Phone page (`assets/captain_bidding/phone.html`)** is plain HTML/CSS/JS (no React, no build step, no Node/npm) styled with the same Calgary Vipers palette as the desktop app (dark background, green primary, red secondary, gold highlight), with large tap targets for +1M/+2M/+5M. It clearly labels itself "TEST MODE — NO TEAM AUTHENTICATION" and MOCK/LIVE mode. It polls `/api/state` every 750ms and renders: no-active-session, BLOCKED, COMPLETE, team-selection (4 buttons, Phase 1 has no auth), and the live bidding view (current player + photo/name/position/OVR/FPL — an unknown FPL value renders "FPL N/A", never an invented number — current bid/leading team, the viewer's own team budget/squad/max-legal-bid, and the three bid buttons). A rejected bid (e.g. below the current highest, or above the team's max legal bid) shows the exact rejection reason returned by `live_bid_service`; it never silently clamps or retries.
- **Sync method: polling, not WebSockets**, chosen for reliability-over-complexity for a one-day trial — every state change (another team's bid, the organizer's own desktop bid, a new current player, SOLD/UNSOLD, auction completion) is picked up within one poll interval (500ms desktop, 750ms phone) with no push infrastructure to debug under time pressure. The API/service boundary does not preclude adding WebSocket push in Phase 2.
- **Persistence: autosave every accepted bid (not periodic/debounced, not transient-until-SOLD).** Measured directly before committing to this: an accepted bid plus a full session autosave took roughly 2.4ms on average (about 2.75ms worst-case observed) against a save file of roughly 17.8KB — cheap enough that "autosave every accepted bid" (the ticket's own stated preference when writes are cheap) was chosen for maximum recovery safety, with no measurable UI responsiveness cost. A rejected bid never triggers an autosave. Bidding state rides inside the exact same `Auction`/session snapshot Milestone 9 already writes — there is no separate bidding save file, and an old Milestone-9 save missing the `current_bid`/`leading_team_id` keys entirely still loads safely (they fall back to their dataclass default of `None` via `from_dict`'s `cls(**values)` pattern).
- **New dependencies** (`requirements.txt`): `fastapi>=0.115,<1.0`, `uvicorn>=0.30,<1.0`, and `httpx>=0.27,<1.0` (test-only — required by FastAPI's `TestClient`, never imported by the running server itself). Verified installing cleanly in this project's Python 3.14 environment.
- **New tests**: `tests/test_live_bid_service.py` (22 tests — rejection of no-session/invalid-team/sub-1M/non-increasing/over-max-legal-bid/budget-reserve/full-roster/GK-conflict bids, correct acceptance and increment behavior, a `threading.Barrier`-based concurrency test proving exactly one of two simultaneous equal-value bids is accepted, and reset-on-SOLD/UNSOLD via the real transaction functions) and `tests/test_captain_bidding_server.py` (26 tests — health, state for no-session/MOCK/LIVE, current-player/FPL/team/max-legal-bid correctness, accepted/rejected bids through the real HTTP API, malformed-payload 422s, path-traversal-safe photo serving, and real bound-port start/stop/repeated-start/repeated-stop lifecycle checks). `tests/test_live_auction_screen.py` gained a "CAPTAIN PHONE BIDDING — PHASE 1" section (desktop bidding through the same service as the API, SOLD pre-fill from a live bid, UNSOLD/SOLD clearing the live bid for the next player, no stale leader after advancement, and manual auction with the server OFF). `tests/test_settings_screen.py` and `tests/test_persistence_service.py` were extended for the new Settings controls and the autosave-per-bid/old-save-compatibility guarantees respectively.
- **Verified end-to-end**: `python -m pytest -q` — **821 passed, 4 skipped, 0 failed** (the same pre-existing, unrelated multi-root-Tk-environment flakiness documented elsewhere in this file — reliable in isolation). No real phone was available in this development environment; in its place, a full protocol-level simulation against a real bound HTTP socket (not just FastAPI's in-process `TestClient`) reproduced the ticket's own example scenario byte-for-byte: phone selects Blackout FC → phone bids 5M → desktop bids Darkstar 6M → phone raises Blackout to 7M → organizer presses SOLD → Blackout FC is charged exactly 7M, budget/roster/history/autosave update correctly, and both the desktop and the simulated phone see the next player with a fully reset bid state. Manual error-path coverage (over-max bid, GK conflict, full roster, non-increasing bid, STOP-server-mid-session followed by a normal desktop-only finish, then restart) is exercised automatically by the test suite above rather than by hand, for the same reason — see "Known issues" below.
- **Phase 2 (documentation only — not implemented, per the ticket's explicit instruction)**: all four captain phones bidding simultaneously; per-team PIN authentication (organizer-generated, resettable) replacing today's open team selector; one authenticated team locked per browser session; a per-team connection-status indicator (not just a total count); an optional QR code to the LAN URL; WebSocket push in place of polling; an organizer control to disconnect/reset a captain's session; a hardened UI for real tournament-day network conditions (reconnect messaging, clearer offline states). None of tournament standings, live tournament FPL scoring, or EXE packaging are part of Phase 2 either — they remain separate, un-scoped future work.

## Key implementation decisions

- Money is stored as integer millions (`14` means 14M) to avoid floating-point currency errors.
- Rosters store canonical player IDs; the captain is always the first initial member.
- Enums constrain player position, player auction status, and overall auction status.
- `Team.add_purchased_player()` protects budget, squad size, and duplicate membership at the model layer.
- Queue creation lives in `services/randomization_service.py`, not in the `Auction` model or any future GUI callback; the model only stores and advances state. Bidding, selling, marking unsold, and persistence still belong to services in a later approved milestone.
- Tournament-specific counts, budgets, squad limits, position colors, and increments are centralized in `data/settings.json`.
- Branding is stored as configuration (`app_name`, `branding_name`, `header_title`, `logo_path`, and `theme_name`) so a later logo or identity change does not require rewriting UI components.
- `services/config_service.py` is the single place that reads `data/settings.json` for branding/position-color values; `ui/theme.py` only holds pure-presentation constants (hex colors, fonts, window sizes) that are not tournament data.
- The UI layer (`ui/`) only calls into `models/` and `services/`; it never contains auction, roster, or persistence logic itself.
- `services/player_service.py` owns player/team loading, search/filter, and setup validation; `ui/screens/players_setup_screen.py` only renders that data — it never parses JSON or reimplements filtering itself.
- `MainWindow` routes by name through `ui/screens/SCREEN_BUILDERS`, a small dict from nav label to screen-builder function, so adding each future real screen means adding one dict entry rather than growing an `if/elif` chain in `MainWindow`.
- `services/team_service.py` owns roster-to-`Player` resolution and derived team stats (`TeamSummary`); it reuses `player_service.load_players()`/`load_teams()` rather than re-reading JSON itself, and never writes back — Live Auction's SOLD/UNSOLD service will be the first thing allowed to mutate `Team` state, in a later milestone.
- Shared, non-screen-specific widgets (colored pill badges, position badges) live in `ui/widgets.py` so `players_setup_screen.py` and `teams_screen.py` (and future screens) render them identically instead of each maintaining its own copy.
- `services/auction_service.py` is the only code allowed to mutate `Team`/`Player`/`Auction` state together; it validates everything before mutating anything (rather than mutating-then-rolling-back), so a rejected transaction is guaranteed to leave every object unchanged with no special-case cleanup code needed.
- `Player.auction_eligible` and `Player.auction_status` are deliberately different things: `auction_eligible` is a fixed category flag decided once (which 28 players were in the original pool), while `auction_status` is the live field (`AVAILABLE` → `SOLD`/`UNSOLD`) that the transaction service updates. Nothing after Milestone 3 changes `auction_eligible`.
- `AuctionSession` (Milestone 8) is the one place allowed to own the "current running auction" — `MainWindow` creates it once and hands the same instance to every screen; no screen loads its own copy of an in-progress auction, which is what makes navigation-without-reshuffling and Teams' live-state view both work for free rather than needing special-case syncing code.
- `AuctionSession.start()` being a no-op when already started (rather than the UI simply hiding the START AUCTION button) means the "never re-randomize" guarantee holds even if a future bug calls it twice — the invariant lives in the service, not in UI button visibility.
- Shared image loading (`ui/widgets.load_image_safely`) is used for both the branding logo (Milestone 4) and player photos (Milestone 8) — one implementation of "load an image, resize preserving aspect ratio, return None instead of raising on any failure," reused rather than duplicated.
- SOLD is the single permanent outcome (Milestone 8.1); UNSOLD is a transient "try again next round" result, not a rejection. `sold_player_ids` (permanent) and `resolved_player_ids` (any history entry, still available but no longer used for the "already done" check) are deliberately two different functions rather than one function whose meaning changed — so any other caller relying on the old "any history entry" semantics doesn't silently break.
- `Auction.round_number`/`start_new_round()` extend the model minimally (same pattern as Milestone 3's `is_initialized`/`advance()`) rather than introducing a second queue/history representation for rounds — a round is still just "the current contents of `queue`", replaced wholesale when a round transitions.
- `AuctionStatus.BLOCKED` is a real, renderable, terminal-for-now state (like `COMPLETE`), not an exception — because the transaction that revealed the blocked condition was itself perfectly valid and already succeeded, only what happens *next* is impossible, so raising would have been the wrong signal.
- `team_can_bid_for_player` deliberately ignores budget (only roster space + the GK rule) since it's used before a price is known (UI eligibility, blocked-state detection); `process_sale` remains the sole source of truth for a specific transaction attempt, including budget.
- `services/live_bid_service.py` (September 2026) is the one place allowed to write `Auction.current_bid`/`leading_team_id`, guarded by a single lock so a bid's validate-then-mutate sequence is atomic; `services/auction_service.py` only ever clears those two fields (on advancement) and never sets them, keeping "who is allowed to set the live bid" and "who is allowed to finalize a sale" as two clearly separate authorities even though both ultimately live in `services/`.

## Known issues / intentional gaps

- Ratings were refreshed and organizer-approved in September 2026 (see "Player Data Refresh — September 2026"), but the organizer may still revise player data before tournament day.
- No sidebar route still shows Milestone 4 placeholder text — Players & Setup, Teams, Live Auction, Auction History, Reports, and (September 2026) Settings all have real screens. Player/team database editing/management remains explicitly deferred; captain phone bidding is now real (Phase 1, September 2026 — see "Captain Phone Bidding — Phase 1 — September 2026") but limited to one unauthenticated captain at a time, with PIN authentication for all four captains simultaneously deferred to Phase 2.
- **Captain Phone Bidding Phase 1 was never tested against a real physical phone in this development environment** (sandboxed, no phone on the same network) — it was instead verified via a full protocol-level simulation against a real bound HTTP socket reproducing the ticket's exact scenario. The organizer should follow the manual test steps in `README.md`'s "Captain Phone Bidding — Local Test" section with a real phone before relying on this for tournament day. Connected-device counting is heartbeat/polling-based (phones seen within the last 5 seconds), not a guaranteed-exact count under all network conditions.
- Auction History and Reports are both entirely read-only by design (see "Auction History — September 2026" and "Reports — September 2026"): no undo, edit, delete, or reassignment of a past transaction exists on either screen. A future organizer recovery workflow (e.g. undoing a mis-entered sale) remains explicitly out of scope, same as the existing BLOCKED-state recovery gap noted above.
- Reports has no PDF/CSV export yet (explicitly deferred) — its data is structured as plain dataclasses (`services/report_service.py`) specifically so an export feature could be added later without restructuring the underlying calculations.
- Players & Setup remains fully read-only by design. Teams remains read-only for manual edits but now reflects live auction purchases when a session is active. Live Auction is the only screen that mutates state, and only via the Milestone 7 transaction service — never directly.
- **Canonical `data/players.json`/`data/teams.json` remain entirely read-only reference data** — nothing, including Milestone 9's persistence layer, ever writes back to them; a running auction's mutable state now lives in `saves/` instead (see "Milestone 9 — Persistence / Autosave / Recovery" below). Closing the application no longer loses a running auction — it autosaves after every state-changing action and can be resumed exactly via the Live Auction screen's session launcher.
- The budget-reserve rule's "team can't legally afford even the minimum price" case (`team_can_bid_for_player` returning `False` purely on budget grounds) should, per the ticket, "normally only arise from invalid/synthetic state" in a properly run tournament, since the rule is enforced continuously at every sale — it's handled safely (contributes to BLOCKED-state detection like any other ineligibility reason) but isn't expected to occur in a real auction where the organizer never bypasses the app.
- No live bidding between teams — one final price is entered per sale by the organizer (with optional +1M/+2M/+5M quick-adjust buttons on the price field itself, not a bid ladder between teams).
- A poorly-managed auction (an organizer who buys GKs/fills rosters without thinking ahead) can genuinely reach a BLOCKED state — this is correct, intentional behavior (see Milestone 8.1), not a bug, but it means there is currently no way to *recover* from a blocked auction within the app (no undo, no manual reassignment) short of restarting the whole session. A recovery/undo workflow remains out of scope, including after Milestone 9 — persistence lets you resume a BLOCKED session exactly as it was, but doesn't add a way to un-block it.
- Milestone 9 has no schema migration: if a save file's `schema_version` doesn't exactly match the app's current `persistence_service.SCHEMA_VERSION`, it's rejected with a clear error rather than upgraded — acceptable for a single-developer, single-deployment tool today, but a future format change would need real migration logic.
- No UI browses archived LIVE sessions (`saves/live/live_archived_*.json`) — they're preserved on disk (never deleted) for manual inspection/backup, but only the current `live_active.json` is surfaced in the session launcher. Only the current MOCK/LIVE saves are listed; nothing prunes old mocks automatically, so a very long test/practice history would need manual cleanup (deleting a mock from the launcher, or the file directly).
- Persistence assumes a single application process at a time — there's no file locking, so two instances of the app pointed at the same `saves/` directory could race on the same save file. Not a concern for the tournament's actual single-organizer, single-laptop usage.
- Windows packaging (PyInstaller or similar) is still not implemented, as instructed — `saves/`, like `data/` and `assets/`, resolves relative to the source tree's `ROOT_DIR` today; a packaged build would need its own writable install-relative (or user-profile) location, which is deliberately out of scope for this milestone.
- The GK-coverage setup check (`validate_setup`) is a static sanity check using `>=`, not a guarantee: it does not simulate the auction, so it cannot promise a *specific* auction run won't still end up BLOCKED (e.g. from filling teams up before they get a GK) — only that there are structurally enough GKs for it to be *possible*. The runtime BLOCKED-state detection is what actually catches a bad outcome.
- Player photos: all 32 canonical players now have a real photo asset under `assets/players/` with a populated `photo_path` (see "Player Photo Integration — September 2026"). The safe-loading/fallback path (missing file, corrupt file, no `photo_path`) remains fully exercised for the cases it still needs to cover — team-purchased-player edge cases, a future roster edit, or a photo file later going missing.
- Player Cards' detail view can require scrolling to see every field on a tall or DPI-scaled display, since it shares the same `load_cover_fit_image`-based photo sizing as every other screen — intentional (a `CTkScrollableFrame` wraps it precisely so nothing clips), not a defect; see "Player Cards + Last-Season FPL — September 2026" for why an in-page panel was used instead of a dialog.
- Last-season FPL points are a display-only statistic sourced from one organizer-supplied result sheet — they play no role in any auction rule (budget, GK, re-auction, sorting a real auction queue) and are not validated against any external FPL data source; a future correction would be another manual `data/players.json` edit, the same as a rating correction.
- Cross-model operations (for example updating Player and Team atomically) are handled by `services/auction_service.py` for the SOLD/UNSOLD case specifically; no other cross-model operation exists yet.
- Base prices are not assigned because the specification provides examples, not final values. The transaction service accepts a final sale price supplied directly by the caller rather than deriving one from a base price or live bidding.
- Team assets/logos are not added; the official branding logo is present. Team cards (Teams screen and Live Auction's team-status row) use plain text/name only, as instructed.
- `Auction.advance()` itself still only moves the queue pointer and does not touch `status`/history — `services/auction_service.py` is what calls `advance()`, writes `AuctionHistoryEntry` records, and syncs `Auction.status` to `COMPLETE`, exactly once per successful transaction.
- GUI-creation tests (`tests/test_ui_shell.py`, `tests/test_players_setup_screen.py`, `tests/test_teams_screen.py`, `tests/test_live_auction_screen.py`) only run when a real display/Tk backend is available; they skip (do not fail) in a headless environment. Most share one session-scoped hidden Tk root (`tests/conftest.py`) to reduce flaky TclError teardown timing from repeatedly creating/destroying CTk() roots in one process — the two tests that build a full `MainWindow` (in `test_ui_shell.py` and `test_live_auction_screen.py`) each still create their own separate root (a screen frame can't substitute for the app's own top-level window) and occasionally skip for the same underlying reason; both pass reliably in isolation.

## Next task (requires approval)

Define Milestone 9 scope before implementation. Do not begin it automatically.
